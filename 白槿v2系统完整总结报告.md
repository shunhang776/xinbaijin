# 白槿 v2 系统完整总结报告

> 2026-06-09，FastAPI + BGE-M3 + FAISS 全本地记忆引擎。68 个 Python 文件，6652 行代码。

---

## 一、项目概况

白槿的数字生命系统，纯 Windows 原生部署。聊天走 DeepSeek API，语义嵌入（BGE-M3 1024维）+ 记忆检索（FAISS + BM25）100% 本地化。

### 核心指标

| | v1（初代） | v2（第一次重写） | v2 当前 |
|------|--------|-----|------|
| Python 文件 | 44 | 25 | 68 |
| 总行数 | ~12,000 | 2,897 | 6,652 |
| 嵌入模型 | 手写 Transformer | all-MiniLM-L6-v2 (384维) | BGE-M3 (1024维) |
| 向量库 | Chroma | Chroma | FAISS + BM25 |
| 记忆引擎 | Mem0 | Chroma | 自研全本地引擎 |
| 端口 | 8080 | 8000 | 8080 |

---

## 二、核心模块

### 2.1 embedding/ — BGE-M3 嵌入模型包（3 文件，86 行）

| 文件 | 行数 | 功能 |
|------|------|------|
| `embedding/__init__.py` | 38 | 预热 + 健康检查，`preload()` / `is_ready()` |
| `embedding/embedder.py` | 42 | BGE-M3 封装，Query/Doc 分锁，线程安全 |
| `embedding/config_embed.py` | 6 | 模型路径 + 参数常量 |

- 模型：BAAI/bge-m3，1024 维向量
- CPU 环境 fp16 关闭
- 启动时阻塞预热，第一个请求零延迟

### 2.2 memory_engine/ — 全本地记忆引擎（21 文件，~2,100 行）

| 文件 | 行数 | 功能 |
|------|------|------|
| `engine.py` | 145 | 引擎入口，单例 |
| `db.py` | 239 | SQLite 数据层 |
| `index_vector.py` | 155 | FAISS 向量索引（1024 维） |
| `index_bm25.py` | 86 | BM25 文本索引 |
| `index_time.py` | 162 | 时间线索引 |
| `index_associative.py` | 48 | 联想索引 |
| `write_pipeline.py` | 238 | 异步写入流水线 |
| `evolve.py` | 234 | 演化引擎（自动整合/压缩） |
| `retrieve_core.py` | 130 | 检索核心 |
| `retrieve.py` | 107 | 检索入口 |
| `retrieve_v2.py` | 58 | 检索 v2 |
| `emotion.py` | 224 | 情绪模块 |
| `active_behavior.py` | 139 | 主动行为决策 |
| `preferences.py` | 113 | 偏好管理 |
| `state.py` | 100 | 状态管理 |
| `reminder.py` | 60 | 提醒管理 |
| `memory_fidelity.py` | 64 | 记忆保真 |
| `recent_chat.py` | 39 | 近期对话缓存 |
| `imperfect_speech.py` | 31 | 口语自然化 |
| `category_store.py` | 81 | 分类存储 |
| `resource_store.py` | 55 | 资源存储 |
| `config.py` / `constants.py` / `utils.py` | — | 配置/常量/工具 |

### 2.3 src/ — 应用层（30+ 文件，~2,000 行）

| 文件 | 行数 | 功能 |
|------|------|------|
| `deepseek.py` | 149 | DeepSeek API 客户端（流式 + 工具调用 + 重试） |
| `tools.py` | 146 | 模型工具定义 + 执行路由 |
| `memory.py` | 143 | 记忆接口（兼容旧调用） |
| `sleep.py` | 151 | 睡眠整合 + 消息队列 |
| `internet.py` | 127 | 联网搜索 + 降级 |
| `vision.py` | 114 | 图片视觉描述 |
| `safety.py` | 104 | 四层安全过滤 |
| `health.py` | 97 | 健康监控面板 |
| `errors.py` | 97 | 全局异常处理 |
| `habits.py` | 91 | 习惯检测 |
| `context.py` | 79 | 上下文智能裁剪 |
| `activity.py` | 102 | 活跃度记录 |
| `conversation.py` | 63 | 对话历史持久化 |
| `identity.py` | 52 | 身份加载 |
| `auth.py` | 72 | QQ Bot ed25519 鉴权 |
| `easter_eggs.py` | 53 | 彩蛋匹配 |
| `compressor.py` | 59 | 记忆压缩 |
| `reminder.py` | 66 | 提醒检查 |
| `sample.py` | 77 | 样本库 |
| `locks.py` | 51 | filelock 原子读写 |
| `src/plugins/` | 8 文件 | 联想/上下文增强/日摘要/事实提取/面具检测/记忆评分/元记忆/主动类型/状态系统 |

### 2.4 prompt/ — Prompt 构建（2 文件，58 行）

| 文件 | 行数 | 功能 |
|------|------|------|
| `builder.py` | 57 | v2 统一 prompt 构建 |

### 2.5 入口 + 运维（3 文件，1,010 行）

| 文件 | 行数 | 功能 |
|------|------|------|
| `main.py` | 677 | FastAPI 入口，QQ Bot Webhook + Claude 桥接 + CLI |
| `bridge.py` | 12 | CLI 客户端 |
| `v2_ops.py` | 321 | 运维守护，端口监控 + 自动重启 + 定时备份 |

---

## 三、架构图

```
用户消息（QQ / Claude Code）
          │
          ▼
    ┌─────────────┐
    │   main.py    │  FastAPI 入口（端口 8080）
    │   /bot       │  QQ Webhook（op=0, C2C_MESSAGE_CREATE）
    │   /bridge    │  Claude 桥接（persona + update）
    └──────┬───────┘
           │
    ┌──────┴──────────┐
    ▼                  ▼
┌─────────┐    ┌────────────────┐
│ DeepSeek│    │ memory_engine   │
│  API    │    │ FAISS + BM25    │
│ + tools │    │ + 时间线 + 联想  │
└─────────┘    └──────┬─────────┘
                      │
              ┌───────┴───────┐
              ▼               ▼
      ┌──────────────┐  ┌──────────┐
      │ embedding/   │  │ SQLite   │
      │ BGE-M3 1024维│  │ 结构化数据│
      └──────────────┘  └──────────┘

后台:
  memory_engine/evolve.py   自动整合/压缩
  memory_engine/write_pipeline.py  异步写入
  v2_ops.py                 30s 巡检 + 自动重启
  src/sleep.py              睡眠消息队列 + 每日整合
```

---

## 四、消息处理流水线

```
用户消息 "加班好累"
  │
  ├── 1. 彩蛋匹配（easter_eggs.match）
  │      → 命中直接返回，不走 LLM
  │
  ├── 2. 构建消息（_build_messages）
  │      prompt/builder.py → 记忆检索 + 人格 + 上下文
  │
  ├── 3. DeepSeek 生成（deepseek.generate_with_tools）
  │      → 工具调用 → 执行 → 结果回注
  │
  ├── 4. 后处理（_post_process）
  │      memory_engine.remember_sync()  → 记忆入库
  │      conversation.append()          → 对话记录
  │      sleep.enqueue()                → 睡眠队列
  │
  └── 5. 自然化（imperfect_speech.add_imperfections）
         → QQ 异步发送（Token 缓存 + 3 次重试）
```

---

## 五、启动流程

```
1. 加载环境变量（.env）
2. 环境检查（DEEPSEEK_API_KEY / QQ_APP_ID / QQ_APP_SECRET）
3. 加载身份配置（identity.load）
4. 加载对话历史（conversation.load）
5. 预加载 BGE-M3 模型（embedding.preload，阻塞 2-5s）
6. 注册全局异常处理 + 健康检查路由
7. 监听 8080 端口
8. 后台异步预热记忆引擎
```

---

## 六、API 端点

| 端点 | 方法 | 说明 |
|------|------|------|
| `/` | GET | 服务状态 |
| `/health` | GET | 模块状态 + DeepSeek/QQ 依赖检测 |
| `/bot` | GET | QQ Bot 验证 |
| `/bot` | POST | QQ Webhook（C2C 消息） |
| `/bridge/persona` | GET | Claude 端获取 persona prompt |
| `/bridge/update` | POST | Claude 端同步对话 + 情绪 + 提醒 |

---

## 七、CLI 命令

```bash
python main.py                      # 启动 FastAPI 服务器
python main.py persona "文本"       # 获取 persona prompt
python main.py update "消息" "回复" -e "心情" [-ev "事件"] [-r "提醒内容"]
python main.py migrate              # 从旧 Mem0 迁移
```

---

## 八、设计铁律

1. **每函数 ≤ 20 行** — 超过就拆
2. **先处理异常** — 异常前置，不吞异常
3. **零硬编码** — 所有配置进常量或环境变量
4. **模块解耦** — 单点故障不牵连全局
5. **删改必清根** — 旧 import、旧路径、旧注释彻底消除
6. **一次一个文件** — 写完等确认再继续
7. **先读再写** — 没读过不编辑
8. **以事实说话** — 不胡编乱造，错了就认

---

## 九、生产环境

- `D:\xinbaijn\main.py` — QQ Bot Webhook（8080 端口）
- `D:\xinbaijn\v2_ops.py` — 运维守护
- `D:\xinbaijn\guard.py` — 守护的守护
- frp 隧道 + 云服务器 nginx — 外网通道
- DeepSeek API — 聊天生成
