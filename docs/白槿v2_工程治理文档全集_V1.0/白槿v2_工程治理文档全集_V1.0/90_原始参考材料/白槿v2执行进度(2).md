# 白槿 v2 执行进度

> 最后更新：2026-06-17

## Phase 1: Foundation（core/ 全模块 + 工具链地基）

| 步骤 | 文件 | 状态 | 备注 |
|------|------|------|------|
| 1.1.1 | `core/__init__.py` | ✅ 完成 | 33 符号导出，CircularDependencyError + is_shutting_down + utc_ts_to_bj/bj_ts_to_utc |
| 1.1.2 | `core/config.py` | ✅ 完成 | Settings 150+ 字段，hot_reload+双回调，enable_file_logging 开关，全 RLock |
| 1.1.3 | `core/constants.py` | ✅ 完成 | 三枚举 + 不可变常量，17 个重复删至 config 唯一真源，PROTECTED_GRADES 保留 enum 版 |
| 1.1.4 | `core/paths.py` | ✅ 完成 | `from __future__` + `__all__` 15 路径常量，DEAD_LETTER_DIR/SNAPSHOT_DIR |
| 1.1.5 | `core/utils.py` | ✅ 完成 | 北京时间戳六函数+utc_ts_to_bj/bj_ts_to_utc 两转换，铁律写入 docstring |
| 1.1.6 | `core/cache.py` | ✅ 完成 | RLock+TTL 抖动双端统一+_key_locks try/finally 清理+CACHE_NULL_TTL 引用+`__init__` 签名取常量 |
| 1.1.7 | `core/metrics.py` | ✅ 完成 | RLock+psutil.Process 缓存+`__all__`，record_error 同步递增 request_total |
| 1.1.8 | `core/logging.py` | ✅ 完成 | int/str 级别兼容+handler 存在时仍更新+enable_file_logging 自动挂载+`__all__` |
| 1.1.9 | `core/types.py` | ✅ 完成 | 5 DTO 全部 frozen+5 Protocol+id 字段标注 noqa |
| 1.1.10 | `core/exceptions.py` | ✅ 完成 | 四层异常，with_context 链式调用，`__str__`/`__repr__` 完整 |
| 1.1.11 | `core/container.py` | ✅ 完成 | RLock+deque O(1)+factory 类型注解+stop_all 默认 SHUTDOWN_DRAIN_TIMEOUT |
| 1.1.12 | `core/signal_handler.py` | ✅ 完成 | RLock+is_shutting_down()+超时兜底+Callable 注解+`__all__` |
| 1.1.13 | `version.py` | ✅ 完成 | `__version__ = "2.0.0"` |
| 1.1.14 | `scripts/ops/lint_imports.py` | ✅ 完成 | 四规则+`__init__.py` 排除+import core.xxx 检测 |
| 1.1.15 | `requirements/` 拆分 | ✅ 完成 | core/server/memory/optional/base/all 六文件，ASCII 注释兼容 pip GBK |

**进度：15/15 ✅ Phase 1 完成**

---

## Phase 1 审查与测试（2026-06-17）

### 审查轮次

| 轮次 | 类型 | 数量 | 覆盖 |
|------|------|------|------|
| 1 | 代码 bug | 10 | Lock→RLock ×3 / 双源冲突 / TTL 抖动 / key_locks 泄漏 / 日志级别 / DTO frozen / psutil 缓存 / deque |
| 2 | 规范加固 | 6 | 魔法数字 / is_shutting_down / 命名空间污染 / 类型注解 / 超时兜底 / `__all__` |
| 3 | 细节修正 | 6 | from __future__ / CircularDependencyError 导出 / factory/logger 注解 / 注释修正 / id 标注 |
| 4 | 注解收尾 | 3 | _key_locks 类型 / factory 类型 / _reload_lock 类型 |
| 5 | 结构性差距 | 6 | 巧合对齐→常量进签名 / 文件日志→开关控制 / 北京时戳→转换函数+铁律 / async→不修 / 零对接→Phase 2 / 零测试→262 测试 |
| 6 | lint 误报 | 1 | `__init__.py` 自身导入排除 |

### 测试覆盖

```
tests/
  test_utils.py             34 passed  时间戳生成+转换+格式化+校验+往返+参数化
  test_cache.py             24 passed  get/set/delete/TTL/LRU/get_or_compute/并发
  test_cache_extreme.py     11 passed  抖动分布/TTL边界/100线程stampede
  test_container.py         24 passed  注册/解析/依赖注入/拓扑/循环检测/生命周期
  test_metrics.py           16 passed  请求/错误/缓存/搜索/Token/快照/重置/并发
  test_exceptions.py        17 passed  层级/构造/__str__/with_context/各层独立
  test_types.py             21 passed  6 DTO frozen 参数化/MemoryItem/Protocol
  test_core_modules.py      37 passed  APIPath/ErrorCode/constants/paths/config/logging
  test_signal_handler.py    14 passed  状态查询/安装幂等/RLock验证/超时常量引用
  test_lint.py              27 passed  stdlib判定/项目模块判定/四规则全覆盖
  test_config_extreme.py    17 passed  hot_reload回调/嵌套字段/边界值/并发热更
  test_logging_extreme.py   20 passed  JSON格式/脱敏/trace_id/文件日志/级别更新
────────────────────────────────────────────────────────────────────
                           262 passed  零跳过，零 xfail
```

### 遗留结构性差距（Phase 2 解决）

| 差距 | 处置 |
|------|------|
| 与现有代码零对接 | Phase 2 第一个里程碑：至少一个模块走通 `from core import` |
| async 兼容 | 不修——后台线程池架构已覆盖，API 路由不直调同步 IO |
| `__init__.py` 被修剪 | 待 Phase 2 根据实际接入需求恢复导出 |

---

## Phase 2: Infrastructure（infra/ 三模块 + 启动文件 + 转发层）

### 2.1 infra/ 模块 + 启动文件

| 步骤 | 文件 | 状态 | 备注 |
|------|------|------|------|
| 2.1.1 | `infra/__init__.py` | ✅ 完成 | 导出 LLMClient, llm_client |
| 2.1.2 | `infra/llm_client/__init__.py` + `client.py` | ✅ 完成 | LLM 统一入口，三级降级（主模型→备用→兜底），熔断器 RLock 热更，指数退避参数化，Token 统计按模型分开，contextvars trace_id |
| 2.1.3 | `infra/database/__init__.py` + `connection.py` + `migrations.py` | ✅ 完成 | SQLite WAL + Windows 降级 DELETE，连接池 max 5 RLock，事务自动包裹，增量迁移框架，北京时戳记录 |
| 2.1.4 | `infra/embedding/__init__.py` + `embedder.py` + `config.py` | ✅ 完成 | BGE-M3 双锁单例，HF 缓存新旧格式兼容，预热，配置从 settings 实时读取 |
| 2.1.5 | `_env_setup.py` | ✅ 完成 | OMP/BLAS/MKL/NUMEXPR 线程控制 + KMP_DUPLICATE_LIB_OK + CUDA_VISIBLE_DEVICES + Protobuf，settings 优先/默认兜底 |
| 2.1.6 | `_launcher.py` | ✅ 完成 | 统一入口，_env_setup 首行 import，ASGI lifespan 管理迁移/容器启停/连接池关闭，uvicorn 信号直注 old_app.router.lifespan_context |

**进度：6/6 ✅ Phase 2.1 完成**

### Phase 2.1 审查与修复

| 轮次 | 问题 | 修复 |
|------|------|------|
| 构建→审查 | P0×2 (get_connection 未绑定, _retry 不接受 timeout) + P1×2 (熔断器全局共用, _jittered_delay 参数不生效) + P2×3 (URL 硬编码, 超时参数硬编码, 嵌入配置固化) + P3×3 (类型注解, __all__, e.message) | 并行修复 6 文件 |
| 第二轮修复 | 熔断器参数热更 + TOOL_TIMEOUT 硬编码 + uvicorn 信号覆盖 | 熔断器 is_open() 接受运行时参数 + tool_timeout @property + ASGI lifespan |
| 第三轮修复 | Starlette 代理方案功能残缺 + app.server 路径无 lifespan | 直注 old_app.router.lifespan_context + app.server 传入 lifespan |
| Phase 2.1 终审 | 🟡 4 项 (LLM 配置字段缺失, 死代码, docstring 不准, 错别字) | config.py 新增 11 个 LLM 字段, 删除 _build_shutdown_callback, 修正文档 |

### Phase 2.1 测试覆盖

```
tests/
  test_infra_database.py    43 tests  连接池/WAL/事务/溢出/健康检查/并发/迁移框架
```

### 下一步

| 步骤 | 内容 | 状态 |
|------|------|------|
| 2.2 | 旧代码转发层（`src/` → `infra/`，`memory_engine/` → `infra/`） | ❌ 未开始 |
| 2.3 | max 深度复核（LLM 熔断/DB 连接/嵌入启动/启动文件） | ❌ 未开始 |
| 2.4 | 验证 + 测试 | ❌ 未开始 |
