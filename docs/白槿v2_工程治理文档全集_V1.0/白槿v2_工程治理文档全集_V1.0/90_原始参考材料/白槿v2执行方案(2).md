# 白槿 v2 重构执行方案（完整版 · 档位精确标注 + 自动切换提醒）

> 基于《白槿v2重构方案.md》终版 v5，逐项覆盖原方案十一~十五章所有内容。
>
> **总原则：** ultracode 负责批量产出代码骨架与标准实现 → max 负责单点高危模块的深度复核与加固。
> **max 铁律：** 每次只针对单个文件/单个逻辑点开启，附带「编码铁律 + 禁止过度思考 + 只复核不发散」约束，避免乱改无关代码。

---

## 🔔 档位切换提醒说明

本文档中每个需要切换档位的位置都有 **🔔 切换提醒**，格式如下：

> 🔔 **切换档位**：`/effort max` — 原因：进入单文件深度复核

**顺航只需在当前步骤完成后、下一步开始前，键入提醒中给出的 `/effort` 命令即可。**

### 全流程档位切换总览

| 步骤序列 | 档位 | 触发条件 |
|----------|------|---------|
| Phase 1.1 | xhigh | 搭建 core/ 骨架（15 文件） |
| Phase 1.2.1 → 1.2.4 | max（逐文件） | 4 个核心文件终审 |
| Phase 1.3 | xhigh | 验证 |
| Phase 2.1 — 2.2 | ultracode | 3 模块 + 2 启动文件 + 转发层 |
| Phase 2.3.1 → 2.3.4 | max（逐文件） | 4 个高可靠节点复核 |
| Phase 2.4 | ultracode | 验证 |
| Phase 3.1 | ultracode | 多索引 + 脚手架 + 模板 |
| Phase 3.2.1 → 3.2.3 | max（逐文件） | 并发安全 + 算法复核 |
| Phase 3.3 | ultracode | 验证 + 压测 |
| Phase 4.1 — 4.2 | ultracode | 核心链路 + features 并行 |
| Phase 4.3.1 → 4.3.3 | max（逐文件） | 写入/检索/演化复核 |
| Phase 4.4 | ultracode | 验证 + 影子对比 |
| Phase 5.1 | ultracode | 6 模块并行迁移 |
| Phase 5.2.1 → 5.2.2 | max（逐文件） | 状态注入 + 状态机复核 |
| Phase 5.3 | ultracode | 验证 + tick() 一致性 |
| Phase 6.1 — 6.2 | ultracode | 服务层 + 领域层 + 脚手架 |
| Phase 6.3.1 → 6.3.2 | max（逐文件） | 注册表 + 工具安全复核 |
| Phase 6.4 | ultracode | 验证 |
| Phase 7.1 — 7.3 | ultracode | 10 插件 + prompt + 脚手架 |
| Phase 7.4.1 → 7.4.2 | max（逐文件） | 注册表调度 + 异常边界复核 |
| Phase 7.5 | ultracode | 验证 + 灰度 |
| Phase 8.1 — 8.2 | ultracode | app + ops 并行拆分 |
| Phase 8.3.1 → 8.3.4 | max（逐文件） | 线程池/生命周期/中间件/兜底复核 |
| Phase 8.4 | ultracode | 全功能验证 |
| Phase 9 | high | 清理旧代码 |
| Phase 10 | ultracode + max | 运行时防护补齐 |

---

## Phase 1: Foundation（core/ 全模块 + 工具链地基）

**主力档位：xhigh（搭骨架）+ max（终审契约）**

> 🔔 **当前档位应为**：`/effort xhigh`

### 1.1 目录与文件创建（xhigh）

| 步骤 | 文件 | 内容 | 档位 |
|------|------|------|------|
| 1.1.1 | `core/__init__.py` | 导出所有 Protocol、DTO、异常类 | xhigh |
| 1.1.2 | `core/config.py` | pydantic-settings 统一配置，分两组（启动只读 + 运行时热更），`FeaturesConfig` 灰度开关，`on_change` 观察者回调。**密钥字段 `__repr__` 只显示首尾 4 字符** | xhigh |
| 1.1.3 | `core/constants.py` | 业务常量、**所有 API 路径常量收口**（禁止硬编码 URL）、错误码枚举 | xhigh |
| 1.1.4 | `core/paths.py` | 文件路径常量，从旧 `memory_engine/constants.py` 迁移 | xhigh |
| 1.1.5 | `core/utils.py` | 时间戳/格式化工具函数，从 `memory_engine/utils.py` 迁移 | xhigh |
| 1.1.6 | `core/cache.py` | TTL+LRU 缓存，三防（穿透/雪崩/击穿），**容量硬上限** | xhigh |
| 1.1.7 | `core/metrics.py` | 轻量指标：请求量/延迟/错误率/Token/检索命中率/缓存命中率/**进程内存** | xhigh |
| 1.1.8 | `core/logging.py` | 结构化 JSON 日志，contextvars 自动取 trace_id，**自动过滤 `api_key`/`secret`/`token` 敏感字段**，**按日期切割 + 默认保留 7 天 + 错误日志单独归档**，生产环境采样 | xhigh |
| 1.1.9 | `core/types.py` | **全项目 DTO 唯一收口** — `Result[T]`（`@dataclass(frozen=True)`）、`PromptData`、`MemoryItem`（`@dataclass(frozen=True)`）、`HealthStatus`、`DomainModule`、`BaseIndex`、`FusionStrategy`、`MetricsSnapshot`、`LifecycleComponent`（详见 max 复核） | xhigh 初稿 → max 终审 |
| 1.1.10 | `core/exceptions.py` | 分层异常 + 错误码前缀 `INF_*`/`MEM_*`/`PLG_*`/`APP_*`，`context: dict` 强制携带 trace_id/span_id（详见 max 复核） | xhigh 初稿 → max 终审 |
| 1.1.11 | `core/container.py` | DI 容器：构造注入 + 生命周期自动编排 + 懒加载 + 拓扑排序 start_all/stop_all（详见 max 复核） | xhigh 初稿 → max 终审 |
| 1.1.12 | `core/signal_handler.py` | 全局信号管理：SIGINT/SIGTERM/Ctrl+C → 触发优雅停止（停止接收→排空队列→关闭 DB/索引/模型→退出），**Windows 适配控制台关闭事件**（详见 max 复核） | xhigh 初稿 → max 终审 |
| 1.1.13 | `version.py`（项目根） | `__version__ = "2.0.0"`，日志/健康接口输出此版本号 | xhigh |
| 1.1.14 | `scripts/ops/lint_imports.py` | **骨架版**：Phase 1 仅启用规则 1（层级依赖） + 规则 3（循环依赖），后续每 Phase 追加规则 | xhigh |
| 1.1.15 | `requirements/` 拆分 | `base.txt`（FastAPI/uvicorn/httpx/sqlite3）、`memory.txt`（faiss-cpu/numpy/jionlp/datasketch）、`embedding.txt`（FlagEmbedding/torch）、`dev.txt`（mypy/pytest）、`vision.txt`（opencv-python） | xhigh |

> ⏸️ **Phase 1.1 完成检查点**：确认 15 项全部产出，`from core import settings, Result` 无报错，然后进入 max 复核阶段。

---

### 1.2 max 深度复核（逐个开启，单文件单次）

> 🔔 **切换档位**：`/effort max` — 原因：进入核心契约终审，每次只针对一个文件，附带「编码铁律 + 禁止过度思考 + 只复核不发散」

| 步骤 | 文件 | max 复核要点 |
|------|------|-------------|
| **1.2.1** | `core/types.py` | 全量 Protocol 接口定义无歧义；`Result[T]` 泛型约束正确 + `@dataclass(frozen=True)`；`PromptData` 字段完整 + `extra` 扩展预留；`BaseIndex`/`FusionStrategy`/`DomainModule`/`LifecycleComponent` 四个 Protocol 的方法签名与调用方一致；`MemoryItem` 使用 `@dataclass(frozen=True)` |

> 🔔 **1.2.1 完成** → 仍在 max，继续 1.2.2（无需切换）

| **1.2.2** | `core/container.py` | 拓扑排序算法正确性；`start_all()` 自下而上、`stop_all()` 逆序自上而下；懒加载组件首次调用触发 init；注册时自动检测 `LifecycleComponent` 实现；启动时校验实现是否符合 Protocol；禁止运行期 `resolve()` 的 lint 规则可被 `lint_imports.py` 检测 |

> 🔔 **1.2.2 完成** → 仍在 max，继续 1.2.3（无需切换）

| **1.2.3** | `core/exceptions.py` | `BaijinBaseError` 基类设计；`InfraError`/`MemoryError`/`PluginError`/`AppError` 子类分层；错误码前缀规范 `INF_`/`MEM_`/`PLG_`/`APP_`；`context: dict` 自动注入 trace_id/span_id（由 logging 框架自动拼入，各层不手动传）；`__str__` 输出格式一致；禁止裸抛异常的 lint 规则 |

> 🔔 **1.2.3 完成** → 仍在 max，继续 1.2.4（无需切换）

| **1.2.4** | `core/signal_handler.py` | 信号注册完整性：SIGINT/SIGTERM 均注册；停止流程正确：停止接收→排空队列(最大等10s)→关闭 DB→关闭索引→关闭模型→退出；Windows 控制台关闭事件适配（`SetConsoleCtrlHandler` 等效逻辑）；`install_signal_handlers()` 幂等（多次调用安全）；所有入口（main.py/daemon.py）统一引入 |

> ⏸️ **Phase 1.2 完成检查点**：4 个核心文件 max 复核全部通过，问题列表已确认修复。

---

### 1.3 验证 + 测试（xhigh）

> 🔔 **切换档位**：`/effort xhigh` — 原因：max 复核完成，切回 xhigh 跑验证

| 步骤 | 验证项 | 标准 |
|------|--------|------|
| 1.3.1 | import 测试 | `from core import settings, Result, BaijinBaseError, DependencyContainer, install_signal_handlers` 无报错 |
| 1.3.2 | 循环依赖检测 | `python scripts/ops/lint_imports.py` — core/ 内部无循环依赖 |
| 1.3.3 | Protocol 匹配 | `container.register(Protocol, Impl)` 启动时校验通过 |
| 1.3.4 | 配置加载 | `Settings()` 实例化成功，启动只读组 + 热更组均可读取；`__repr__` 密钥脱敏生效 |
| **1.3.5** | **单元测试** | `core/types.py` 的 `Result[T]`：①正常构造 ok=True ②错误构造 ok=False+error_code ③frozen 不可变验证 |
| **1.3.6** | **单元测试** | `core/exceptions.py`：①抛异常含 context ②`__str__` 输出含错误码 ③异常链 correctly wrapped |
| **1.3.7** | **单元测试** | `core/cache.py`：①正常读写 ②TTL 过期 ③容量上限驱逐 ④空值缓存防穿透 |

### Phase 1 回滚条件
- 任一 core 模块 import 失败
- 循环依赖检测不通过
- 回滚方式：删除 `core/` 目录，恢复旧 import 路径

---

## Phase 2: Infrastructure（infra/ 三模块 + 启动文件）

**主力档位：ultracode（3 模块并行实现）**

> 🔔 **切换档位**：`/effort ultracode` — 原因：infra 模块可并行生成，ultracode 批量产出骨架

### 2.1 并行实现（ultracode — 多个 agent 并行）

| 步骤 | 文件 | 内容 | 档位 |
|------|------|------|------|
| 2.1.1 | `infra/__init__.py` | 仅导出 Protocol 接口 | ultracode |
| 2.1.2 | `infra/llm_client/__init__.py` + `client.py` | 全项目唯一 LLM 入口，三级降级熔断，指数退避重试，Token 统计，contextvars 自动携带 trace_id（详见 max 复核） | ultracode 初稿 → max 终审 |
| 2.1.3 | `infra/database/__init__.py` + `connection.py` + `migrations.py` | SQLite WAL + 增量迁移，Windows 兼容兜底，连接池限制，定时 WAL checkpoint 归档（详见 max 复核） | ultracode 初稿 → max 终审 |
| 2.1.4 | `infra/embedding/__init__.py` + `embedder.py` + `config.py` | BGE-M3 嵌入（详见 max 复核） | ultracode 初稿 → max 终审 |
| **2.1.5** | **`_launcher.py`**（项目根） | **第一行**设 `OMP_NUM_THREADS=1` + `OPENBLAS_NUM_THREADS=1`，早于所有 import；Windows 下模型加载路径处理；统一入口（详见 max 复核） | ultracode 初稿 → max 终审 |
| **2.1.6** | **`_env_setup.py`**（项目根） | 环境变量前置设置，在 `_launcher.py` 中最先调用 | ultracode |

### 2.2 旧代码转发层（xhigh）

| 步骤 | 内容 |
|------|------|
| 2.2.1 | 旧 `src/` 中的 LLM 调用改为 `from infra.llm_client import llm_client` 转发 |
| 2.2.2 | 旧 `memory_engine/db.py` 改为 `from infra.database import get_connection` 转发 |
| 2.2.3 | 旧 `embedding/` 改为 `from infra.embedding import embedder` 转发 |

> ⏸️ **Phase 2.1-2.2 完成检查点**：6 个 infra 文件 + 转发层全部产出，import 无报错。

---

### 2.3 max 深度复核（逐个开启）

> 🔔 **切换档位**：`/effort max` — 原因：LLM 熔断/DB 连接/嵌入启动/启动文件 四个高可靠节点需单独深度复核

| 步骤 | 文件 | max 复核要点 |
|------|------|-------------|
| **2.3.1** | `infra/llm_client/client.py` | 三级降级链路完整：①主模型失败→备用模型/降参数 ②全失败→本地规则极简兜底 ③连续熔断≥3次→短时限流30s；指数退避参数正确（base=1s, max=60s）；超时控制覆盖所有外部调用（超时值从 `core/config` 热更组读取）；Token 统计按模型维度分开；并发安全：多线程同时调用不共享可变状态；`contextvars` trace_id 自动携带验证 |

> 🔔 **2.3.1 完成** → 仍在 max，继续 2.3.2（无需切换）

| **2.3.2** | `infra/database/connection.py` | WAL 模式检测：Windows 不兼容时自动降级 DELETE；`busy_timeout=5000` 多线程锁等待；`synchronous=NORMAL` 平衡策略；连接池最大 5 连接；`wal_autocheckpoint=1000`；关键写入显式事务；定时 WAL checkpoint 归档 |

> 🔔 **2.3.2 完成** → 仍在 max，继续 2.3.3（无需切换）

| **2.3.3** | `infra/embedding/` 启动逻辑 | 线程安全初始化（单例 + RLock）；模型加载路径跨平台兼容；OMP/OPENBLAS 环境变量已前置设置 |

> 🔔 **2.3.3 完成** → 仍在 max，继续 2.3.4（无需切换）

| **2.3.4** | `_launcher.py` + `_env_setup.py` | `_launcher.py` 第一行确为环境变量设置，早于所有 import；`_env_setup.py` 设置完整（OMP/OPENBLAS + 其他必要变量）；Windows 下路径处理正确；所有入口统一经 `_launcher.py` |

> ⏸️ **Phase 2.3 完成检查点**：4 个高可靠节点 max 复核全部通过。

---

### 2.4 验证 + 测试（ultracode）

> 🔔 **切换档位**：`/effort ultracode` — 原因：max 复核完成，切回 ultracode 跑验证

| 步骤 | 验证项 | 标准 |
|------|--------|------|
| 2.4.1 | LLM 影子对比 | 新 LLM 客户端 1% 流量 → 输出与旧客户端一致 |
| 2.4.2 | DB 连接 | 无锁等待，WAL 模式正常（或 Windows 下正确降级 DELETE） |
| 2.4.3 | 嵌入加载 | 无线程冲突，模型加载成功，`OMP_NUM_THREADS` 生效 |
| 2.4.4 | 敏感信息脱敏 | `config.py` 密钥 `__repr__` 只显示首尾 4 字符；`logging.py` 自动过滤 `api_key`/`secret`/`token` |
| **2.4.5** | **单元测试** | `llm_client`：①正常调用返回 ②超时→降级 ③全部不可用→兜底 ④连续熔断→限流 |
| **2.4.6** | **单元测试** | `database`：①正常连接 ②WAL 降级 ③连接池耗尽→等待 ④迁移执行 |
| **2.4.7** | **单元测试** | `embedding`：①正常编码 ②空文本 ③模型未加载→异常 |

### Phase 2 回滚条件
- LLM 影子对比错误率 > 5%
- DB 连接死锁
- 嵌入加载 crash
- 回滚方式：特性开关切回旧 LLM/DB/嵌入路径

---

## Phase 3: Indexes + Stores（memory/_indexes/ + _stores/ + 脚手架）

**主力档位：ultracode（多索引并行实现）**

> 🔔 **确认档位**：应仍在 ultracode（Phase 2 验证结束时已切回），如不是请 `/effort ultracode`

### 3.1 并行实现（ultracode — 多个 agent 并行）

| 步骤 | 文件 | 内容 | 档位 |
|------|------|------|------|
| 3.1.1 | `memory/_indexes/__init__.py` | 导出 `INDEX_REGISTRY` | ultracode |
| 3.1.2 | `memory/_indexes/base.py` | `BaseIndex` Protocol 基类，`INDEX_REGISTRY` 注册表 | ultracode |
| 3.1.3 | `memory/_indexes/vector.py` | FAISS 向量索引，RWLock 读写锁（详见 max 复核） | ultracode 初稿 → max 终审 |
| 3.1.4 | `memory/_indexes/bm25.py` | BM25 关键词索引 | ultracode |
| 3.1.5 | `memory/_indexes/time.py` | 时间衰减索引 | ultracode |
| 3.1.6 | `memory/_indexes/associative.py` | 关联索引 | ultracode |
| 3.1.7 | `memory/_indexes/fusion.py` | 多索引融合排序算法、权重计算（详见 max 复核） | ultracode 初稿 → max 终审 |
| 3.1.8 | `memory/_stores/__init__.py` | 导出存储接口 | ultracode |
| 3.1.9 | `memory/_stores/category.py` | 类别存储 | ultracode |
| 3.1.10 | `memory/_stores/resource.py` | 资源存储，含数据版本自动迁移器 `_MIGRATORS` | ultracode |
| **3.1.11** | **`scripts/ops/gen_index.py`** | 脚手架：交互式生成新索引模块 | ultracode |
| **3.1.12** | **`templates/index_template.py`** + **`test_index_template.py`** | 索引模块模板 + 测试模板 | ultracode |

> ⏸️ **Phase 3.1 完成检查点**：12 项全部产出。

---

### 3.2 max 深度复核（逐个开启）

> 🔔 **切换档位**：`/effort max` — 原因：FAISS 并发安全 + 融合算法 + 存储事务 三个高风险点需 max 单独复核

| 步骤 | 文件 | max 复核要点 |
|------|------|-------------|
| **3.2.1** | `memory/_indexes/vector.py` | FAISS 读写锁实现：必须用 `threading.RLock`（项目铁律）；读多写少场景 RWLock 正确实现；并发读写安全：读不阻塞读，写阻塞读写；`add()`/`remove()`/`search()` 的锁粒度正确；rebuild 时全量重建的原子性；**大索引重建做内存预估** |

> 🔔 **3.2.1 完成** → 仍在 max，继续 3.2.2（无需切换）

| **3.2.2** | `memory/_indexes/fusion.py` | 融合排序算法正确性：多索引结果合并后按权重+分数排序；权重可配且默认值合理；`FusionStrategy` Protocol 实现完整；边界条件：空结果、单索引结果、分数相同排序稳定 |

> 🔔 **3.2.2 完成** → 仍在 max，继续 3.2.3（无需切换）

| **3.2.3** | `memory/_stores/` 并发写入 | 资源锁使用 `RLock`；事务边界正确（写入要么全成功要么全回滚）；`_MIGRATORS` 版本迁移逻辑：旧版本→新版本字段映射正确；双写新旧索引逻辑 |

> ⏸️ **Phase 3.2 完成检查点**：并发安全 + 融合算法 + 存储事务复核全部通过。

---

### 3.3 验证 + 测试（ultracode）

> 🔔 **切换档位**：`/effort ultracode` — 原因：max 复核完成，切回 ultracode 跑并发压测验证

| 步骤 | 验证项 | 标准 |
|------|--------|------|
| 3.3.1 | 各索引可用 | vector/bm25/time/associative 四个索引 `add()` + `search()` 正常 |
| 3.3.2 | 融合检索 | `fusion.search(query, top_k=10)` 返回多索引融合结果 |
| 3.3.3 | 双写一致性 | 新旧索引同时写入，读新补旧，无数据丢失 |
| 3.3.4 | 并发压测 | 10 线程同时读写 vector 索引，无脏读、无死锁 |
| **3.3.5** | **单元测试** | `vector.py`：①正常 add+search ②并发读写安全 ③空索引查询 ④rebuild |
| **3.3.6** | **单元测试** | `fusion.py`：①正常融合 ②单索引 ③空结果 ④权重配置 |
| **3.3.7** | **单元测试** | `resource.py`：①版本迁移 v1→v2 ②迁移字段完整性 ③旧版本读取 |

### Phase 3 回滚条件
- 索引写入数据丢失
- 并发压测出现死锁
- 融合排序结果与旧系统差异 > 20%
- 回滚方式：索引特性开关切回旧索引路径

---

## Phase 4: Core + Engine + Features（记忆核心层 + 离线缓存基础）

**主力档位：ultracode（features 批量迁移）**

> 🔔 **确认档位**：应仍在 ultracode（Phase 3 验证结束时已切回），如不是请 `/effort ultracode`

### 4.1 核心链路实现（ultracode 并行）

| 步骤 | 文件 | 内容 | 档位 |
|------|------|------|------|
| 4.1.1 | `memory/__init__.py` | 仅导出 Protocol + features 公共接口 | ultracode |
| 4.1.2 | `memory/engine.py` | ≤50 行纯转发层 | ultracode |
| 4.1.3 | `memory/_core/__init__.py` | 内部子包导出 | ultracode |
| 4.1.4 | `memory/_core/retrieve.py` | 检索链路编排、融合策略调用、分级保护（详见 max 复核） | ultracode 初稿 → max 终审 |
| 4.1.5 | `memory/_core/write.py` | 异步写入队列、独立线程池、死信队列闭环、优雅排空（详见 max 复核） | ultracode 初稿 → max 终审 |
| 4.1.6 | `memory/_core/evolve.py` | 记忆压缩 + 演化算法（含 compressor 并入）（详见 max 复核） | ultracode 初稿 → max 终审 |
| 4.1.7 | `memory/_core/cooccurrence.py` | 共现图计算 | ultracode |
| 4.1.8 | `memory/_core/background.py` | 重计算后台化定时任务 | ultracode |

### 4.2 Features 批量迁移（ultracode — 6 个 agent 并行）

| 步骤 | 文件 | 来源 | 档位 |
|------|------|------|------|
| 4.2.1 | `memory/features/__init__.py` | 新建 | ultracode |
| 4.2.2 | `memory/features/search.py` | `memory_engine/retrieve.py` | ultracode |
| 4.2.3 | `memory/features/emotion.py` | `memory_engine/emotion.py` | ultracode |
| 4.2.4 | `memory/features/preferences.py` | `memory_engine/preferences.py` | ultracode |
| 4.2.5 | `memory/features/reminder.py` | `memory_engine/reminder.py` | ultracode |
| 4.2.6 | `memory/features/recent_chat.py` | `memory_engine/recent_chat.py`，**缓存强制硬上限** | ultracode |
| 4.2.7 | `memory/features/fidelity.py` | `memory_engine/memory_fidelity.py` | ultracode |

### 4.2-bis Lint 规则增强（xhigh）

| 步骤 | 内容 |
|------|------|
| 4.2.8 | `lint_imports.py` 追加规则 2（私有包保护：禁止外部 import `_` 前缀子包，同包内部可互导） |

> ⏸️ **Phase 4.1-4.2 完成检查点**：8 个核心文件 + 7 个 feature 文件 + lint 规则增强全部产出。

---

### 4.3 max 深度复核（逐个开启）

> 🔔 **切换档位**：`/effort max` — 原因：写入异步队列/检索链路/演化算法 三个核心链路需 max 逐文件复核

| 步骤 | 文件 | max 复核要点 |
|------|------|-------------|
| **4.3.1** | `memory/_core/write.py` | 异步写入队列：独立线程池大小合理；`stop(timeout)` 先停接收→排空队列→退出，保证不丢记忆；死信队列闭环：失败→持久化 JSONL→每 5 分钟重试→累计 ≥3 次归档+告警→>100 条 degraded；写入幂等性（服务重启不重复写入） |

> 🔔 **4.3.1 完成** → 仍在 max，继续 4.3.2（无需切换）

| **4.3.2** | `memory/_core/retrieve.py` | 检索链路编排：索引选择→融合策略→分级保护→结果返回，全链路无遗漏；分级保护逻辑：不同等级记忆的访问控制正确；超时控制：检索超时不影响其他模块，超时值从 `core/config` 读取 |

> 🔔 **4.3.2 完成** → 仍在 max，继续 4.3.3（无需切换）

| **4.3.3** | `memory/_core/evolve.py` | 记忆压缩算法：压缩前后语义一致性；演化触发条件合理；compressor 并入后接口一致；压缩不丢失关键记忆 |

> ⏸️ **Phase 4.3 完成检查点**：写入/检索/演化三个核心链路 max 复核全部通过。

---

### 4.4 验证 + 测试（ultracode）

> 🔔 **切换档位**：`/effort ultracode` — 原因：max 复核完成，切回 ultracode 跑检索影子对比和死信测试

| 步骤 | 验证项 | 标准 |
|------|--------|------|
| 4.4.1 | 检索影子对比 | `engine.search_sync()` 新旧结果 diff < 5% |
| 4.4.2 | 写入无丢失 | 异步队列关闭后所有写入落盘 |
| 4.4.3 | 死信队列 | 模拟写入失败 → 死信队列正确归档 → 重试逻辑正常 |
| 4.4.4 | 异步队列无死锁 | 高频写入 + 关闭，无线程卡死 |
| **4.4.5** | **单元测试** | `write.py`：①正常写入 ②队列关闭排空 ③死信归档 ④重试逻辑 ⑤幂等性 |
| **4.4.6** | **单元测试** | `retrieve.py`：①正常检索 ②空结果 ③分级保护 ④超时处理(mock) |
| **4.4.7** | **单元测试** | `evolve.py`：①压缩前后语义一致 ②触发条件边界 ③compressor 接口兼容 |

### Phase 4 回滚条件
- 检索影子对比差异 > 10%
- 写入队列关闭时数据丢失
- 死信队列未归档
- 回滚方式：检索/写入特性开关切回旧链路

---

## Phase 5: Personality（人格 6 模块）

**主力档位：ultracode（6 模块并行迁移）**

> 🔔 **确认档位**：应仍在 ultracode（Phase 4 验证结束时已切回），如不是请 `/effort ultracode`

### 5.1 并行迁移（ultracode — 6 个 agent 并行）

| 步骤 | 文件 | 来源 | 档位 |
|------|------|------|------|
| 5.1.1 | `personality/__init__.py` | 新建 | ultracode |
| 5.1.2 | `personality/state.py` | `memory_engine/state.py`，改为构造注入拿 activity 数据，含 `_save_snapshot()` + `_restore_snapshot()`（详见 max 复核） | ultracode 初稿 → max 终审 |
| 5.1.3 | `personality/senses.py` | `memory_engine/senses.py` | ultracode |
| 5.1.4 | `personality/life_stream.py` | `memory_engine/life_stream.py` | ultracode |
| 5.1.5 | `personality/idle_thoughts.py` | `memory_engine/idle_thoughts.py` | ultracode |
| 5.1.6 | `personality/desire.py` | `memory_engine/desire_system.py` | ultracode |
| 5.1.7 | `personality/common_sense.py` | `memory_engine/common_sense.py` | ultracode |

### 5.1-bis Lint 规则增强（xhigh）

| 步骤 | 内容 |
|------|------|
| 5.1.8 | `lint_imports.py` 追加规则 4（`__init__.py` 禁止导出实现类） |

> ⏸️ **Phase 5.1 完成检查点**：7 个人格模块文件 + lint 增强全部产出。

---

### 5.2 max 深度复核（逐个开启）

> 🔔 **切换档位**：`/effort max` — 原因：状态注入规范 + 欲望/生活流状态机 两个高风险点需 max 复核

| 步骤 | 文件 | max 复核要点 |
|------|------|-------------|
| **5.2.1** | `personality/state.py` | 构造注入规范：`init(activity_provider)` 在启动时调用一次，运行期只用已注入实例；`_save_snapshot()` 定时快照到 `data/personality_state.json`；`_restore_snapshot()` 启动恢复能量/心情/深度指数；线程安全：状态读写用 RLock；状态字段完整（不丢旧系统任何状态属性）；重启后不"失忆式重置" |

> 🔔 **5.2.1 完成** → 仍在 max，继续 5.2.2（无需切换）

| **5.2.2** | 核心状态机流转 | `personality/desire.py` 欲望系统：欲望生成→衰减→满足→新欲望的流转逻辑边界；`personality/life_stream.py` 生活流：活动触发→持续→结束的状态变化；所有 tick() 方法的输入输出与旧系统一致 |

> ⏸️ **Phase 5.2 完成检查点**：状态注入 + 状态机流转复核全部通过。

---

### 5.3 验证 + 测试（ultracode）

> 🔔 **切换档位**：`/effort ultracode` — 原因：max 复核完成，切回 ultracode 跑 tick() 一致性验证

| 步骤 | 验证项 | 标准 |
|------|--------|------|
| 5.3.1 | tick() 一致性 | `state.tick()` / `desire.tick()` / `life_stream.tick()` 输出与旧系统 diff < 1% |
| 5.3.2 | 状态持久化 | 快照保存→重启→恢复，无状态漂移 |
| 5.3.3 | 构造注入 | `personality/state.py` 运行期无容器调用 |
| **5.3.4** | **单元测试** | `state.py`：①正常 tick ②快照保存→恢复 ③构造注入后运行期不调容器 ④线程安全 |
| **5.3.5** | **单元测试** | `desire.py`：①欲望生成 ②衰减 ③满足后新欲望 ④边界（无欲望） |
| **5.3.6** | **单元测试** | `life_stream.py`：①活动触发 ②持续中状态 ③结束后状态变化 |

### Phase 5 回滚条件
- tick() 输出差异 > 5%
- 状态持久化恢复后关键字段丢失
- 回滚方式：DI 注入点切回旧 state 模块

---

## Phase 6: Services + Domain（服务层 + 领域层 + 脚手架）

**主力档位：ultracode（批量领域模块实现）**

> 🔔 **确认档位**：应仍在 ultracode（Phase 5 验证结束时已切回），如不是请 `/effort ultracode`

### 6.1 Services 层（ultracode 并行）

| 步骤 | 文件 | 来源 | 档位 |
|------|------|------|------|
| 6.1.1 | `services/__init__.py` | 新建 | ultracode |
| 6.1.2 | `services/internet.py` | `src/internet.py`，超时值从 `core/config` 读取 | ultracode |
| 6.1.3 | `services/safety.py` | `src/safety.py` | ultracode |
| 6.1.4 | `services/vision.py` | `src/vision.py` | ultracode |

### 6.2 Domain 层 + 脚手架（ultracode 并行）

| 步骤 | 文件 | 来源/内容 | 档位 |
|------|------|------|------|
| 6.2.1 | `domain/__init__.py` | 新建 | ultracode |
| 6.2.2 | `domain/registry.py` | 领域模块拓扑排序、循环依赖检测（详见 max 复核） | ultracode 初稿 → max 终审 |
| 6.2.3 | `domain/identity/__init__.py` + `loader.py` + `editor.py` | `src/identity.py` + `src/tools.py` 身份编辑部分 | ultracode |
| 6.2.4 | `domain/activity.py` | `src/activity.py` | ultracode |
| 6.2.5 | `domain/habits.py` | `src/habits.py` | ultracode |
| 6.2.6 | `domain/easter_eggs.py` | `src/easter_eggs.py` | ultracode |
| 6.2.7 | `domain/tools/__init__.py` + `definitions.py` + `executor.py` | `src/tools.py` 拆分（详见 max 复核） | ultracode 初稿 → max 终审 |
| **6.2.8** | **`scripts/ops/gen_domain.py`** | 脚手架：交互式生成新领域模块 | ultracode |
| **6.2.9** | **`templates/domain_template.py`** + **`test_domain_template.py`** | 领域模块模板 + 测试模板 | ultracode |

### 6.2-bis Lint 规则增强（xhigh）

| 步骤 | 内容 |
|------|------|
| 6.2.10 | `lint_imports.py` 追加规则 5（禁止运行期调 `DependencyContainer.resolve()`） |

> ⏸️ **Phase 6.1-6.2 完成检查点**：4 个 service + 10 个 domain/脚手架 + lint 增强全部产出。

---

### 6.3 max 深度复核（逐个开启）

> 🔔 **切换档位**：`/effort max` — 原因：注册表调度逻辑 + 工具执行安全 两个节点需 max 复核

| 步骤 | 文件 | max 复核要点 |
|------|------|-------------|
| **6.3.1** | `domain/registry.py` | 拓扑排序正确性：按 priority 排序；循环依赖检测：启动时发现循环依赖即报错；显式注册 + 自动发现(dev 模式)双模式；模块 `get_context()` 和 `get_tools()` 返回值格式规范 |

> 🔔 **6.3.1 完成** → 仍在 max，继续 6.3.2（无需切换）

| **6.3.2** | `domain/tools/executor.py` | 工具调用安全：参数校验→权限检查→执行→结果收敛；异常绝不向上抛，统一返回 `Result[T]`；工具注册去重；超时控制（超时值从 `core/config` 读取） |

> ⏸️ **Phase 6.3 完成检查点**：注册表 + 工具执行复核全部通过。

---

### 6.4 验证 + 测试（ultracode）

> 🔔 **切换档位**：`/effort ultracode` — 原因：max 复核完成，切回 ultracode 跑注册验证

| 步骤 | 验证项 | 标准 |
|------|--------|------|
| 6.4.1 | 模块注册无冲突 | 所有领域模块注册成功，无重名 |
| 6.4.2 | 工具调用规范 | `executor.execute()` 返回 `Result[T]`，无异常泄露 |
| 6.4.3 | search / match_today | 领域搜索功能与旧系统一致 |
| **6.4.4** | **单元测试** | `registry.py`：①正常注册 ②重名检测 ③循环依赖检测 ④自动发现 |
| **6.4.5** | **单元测试** | `executor.py`：①正常执行 ②参数非法→Result(false) ③超时处理 ④异常不泄露 |

### Phase 6 回滚条件
- 领域模块注册循环依赖
- 工具调用异常泄露
- 回滚方式：领域注册表切回旧模块加载路径

---

## Phase 7: Plugins + Prompt（插件 + 提示词 + 脚手架 + 影子流量基础）

**主力档位：ultracode（10 插件并行实现）**

> 🔔 **确认档位**：应仍在 ultracode（Phase 6 验证结束时已切回），如不是请 `/effort ultracode`

### 7.1 插件注册表（ultracode）

| 步骤 | 文件 | 内容 | 档位 |
|------|------|------|------|
| 7.1.1 | `plugins/__init__.py` | 导出注册表 | ultracode |
| 7.1.2 | `plugins/registry.py` | `@register` 装饰器 + pydantic 入参校验 + 重名检测 + 依赖拓扑排序 + 中间件（trace_id 注入→耗时统计→慢调用告警→异常捕获→返回 Result）（详见 max 复核） | ultracode 初稿 → max 终审 |

### 7.2 10 插件并行迁移（ultracode — 10 个 agent 并行）

| 步骤 | 文件 | 内容 | 档位 |
|------|------|------|------|
| 7.2.1 | `plugins/speech_polish.py` | 不完美表达修饰（从 domain 移入） | ultracode |
| 7.2.2 | `plugins/recall.py` | 记忆召回插件 | ultracode |
| 7.2.3 | `plugins/remember.py` | 记忆写入插件 | ultracode |
| 7.2.4 | `plugins/forget.py` | 记忆遗忘插件 | ultracode |
| 7.2.5 | `plugins/search_web.py` | 联网搜索插件 | ultracode |
| 7.2.6 | `plugins/remind.py` | 提醒插件 | ultracode |
| 7.2.7 | `plugins/intuition.py` | 直觉插件 | ultracode |
| 7.2.8 | `plugins/emotion.py` | 情感插件 | ultracode |
| 7.2.9 | `plugins/identity.py` | 身份插件 | ultracode |
| 7.2.10 | `plugins/reflect.py` | 反思插件 | ultracode |

### 7.3 Prompt 层 + 脚手架（ultracode）

| 步骤 | 文件 | 内容 | 档位 |
|------|------|------|------|
| 7.3.1 | `prompt/__init__.py` + `builder.py` | Jinja2 纯函数渲染，无副作用 | ultracode |
| 7.3.2 | `prompt/templates/v1/system.j2` | Jinja2 模板，热加载，AB 测试从 feature flag 控制 | ultracode |
| **7.3.3** | **`scripts/ops/gen_plugin.py`** | 脚手架：交互式生成新插件 | ultracode |
| **7.3.4** | **`templates/plugin_template.py`** + **`test_plugin_template.py`** | 插件模板 + 测试模板 | ultracode |
| **7.3.5** | **影子流量隔离基础** | `core/config` 增加 `shadow_mode` 特性开关；插件中间件增加影子模式标记（独立日志 + 独立指标，只执行不返回） | ultracode |

### 7.3-bis Lint 规则增强（xhigh）

| 步骤 | 内容 |
|------|------|
| 7.3.6 | `lint_imports.py` 追加规则 7（禁止硬编码 URL：所有 API 路径必须引用 `core.constants`） |
| 7.3.7 | `lint_imports.py` 追加规则 6（禁止裸异常捕获：`except:` 裸捕获视为违规） |

> ⏸️ **Phase 7.1-7.3 完成检查点**：1 个注册表 + 10 个插件 + 5 个 prompt/脚手架/影子流量 + 2 条 lint 规则，全部产出。

---

### 7.4 max 深度复核（逐个开启）

> 🔔 **切换档位**：`/effort max` — 原因：插件注册表调度 + 全局异常边界 两个节点需 max 复核（**此阶段最关键，插件异常泄露会导致全线崩溃**）

| 步骤 | 文件 | max 复核要点 |
|------|------|-------------|
| **7.4.1** | `plugins/registry.py` | 插件依赖拓扑调度：`execute_plugins()` 无依赖并发、有依赖按拓扑序串行；启动时循环依赖检测；重名检测抛 `PLG_DUPLICATE_NAME`；pydantic 入参校验正确；中间件链：trace_id 注入→耗时统计→慢调用告警（默认阈值 500ms）→异常捕获→返回 Result；所有异常绝不向上抛；影子模式标记正确注入 |

> 🔔 **7.4.1 完成** → 仍在 max，继续 7.4.2（无需切换）

| **7.4.2** | 全局异常边界 | 逐个检查 10 个插件：确保所有插件只返回 `Result[T]`，无一向上抛异常；debug 日志记录异常详情；影子模式下插件输出不进入实际响应流 |

> ⏸️ **Phase 7.4 完成检查点**：注册表调度 + 全局异常边界复核全部通过。**此检查点至关重要，通过后才能进入灰度。**

---

### 7.5 验证 + 测试（ultracode）

> 🔔 **切换档位**：`/effort ultracode` — 原因：max 复核完成，切回 ultracode 跑灰度验证

| 步骤 | 验证项 | 标准 |
|------|--------|------|
| 7.5.1 | 插件执行无异常泄露 | 模拟各插件异常 → 全部返回 `Result(ok=False)` |
| 7.5.2 | `build_prompt` 一致性 | `build_prompt(data)` 输出与旧系统 diff < 1% |
| 7.5.3 | 流量灰度 1%→10%→100% | 每阶段观察错误率 < 5%，prompt 差异率 < 10%，延迟 < 2x |
| 7.5.4 | 依赖拓扑 | 有依赖声明的插件按正确顺序执行 |
| 7.5.5 | 影子流量 | 影子模式开启 → 独立日志记录 → 不影响实际响应 → 指标独立统计 |
| **7.5.6** | **单元测试** | `registry.py`：①注册+执行 ②重名报错 ③循环依赖报错 ④pydantic 校验 ⑤中间件异常不泄露 |
| **7.5.7** | **单元测试** | 每个插件（10 个）：①正常返回 Result(true) ②异常返回 Result(false) ③不抛异常 |
| **7.5.8** | **单元测试** | `builder.py`：①正常渲染 ②空 PromptData ③extra 扩展字段 ④模板热加载 |

### Phase 7 回滚条件
- 插件异常泄露到上层
- prompt 差异率 > 10%
- 任一灰度阶段错误率 > 5%
- 回滚方式：特性开关切回 0%，秒级生效

---

## Phase 8: App + Ops（应用层 + 运维 + 安全加固收尾）

**主力档位：ultracode（多文件并行拆分）**

> 🔔 **确认档位**：应仍在 ultracode（Phase 7 验证结束时已切回），如不是请 `/effort ultracode`

### 8.1 App 层拆分（ultracode 并行）

| 步骤 | 文件 | 内容 | 档位 |
|------|------|------|------|
| 8.1.1 | `app/__init__.py` | 新建 | ultracode |
| 8.1.2 | `app/server.py` | FastAPI + lifespan 优雅启停逆序编排，引入 `install_signal_handlers()`（详见 max 复核） | ultracode 初稿 → max 终审 |
| 8.1.3 | `app/middleware.py` | trace_id 注入(contextvars) + QQ Bot 鉴权(auth.py 并入) + 本地守卫 + **接口限流（按 QQ 用户/客户端 IP）**（详见 max 复核） | ultracode 初稿 → max 终审 |
| 8.1.4 | `app/routes/__init__.py` | 统一注册所有路由，不漏不重 | ultracode |
| 8.1.5 | `app/routes/bot.py` | QQ Webhook | ultracode |
| 8.1.6 | `app/routes/bridge.py` | Claude 桥接 | ultracode |
| 8.1.7 | `app/routes/memory_api.py` | 记忆 API | ultracode |
| 8.1.8 | `app/routes/health.py` | liveness + readiness 双检查，10s TTL 缓存，**健康评分（0-100）+ degraded 分级（light/heavy）+ 进程内存指标** | ultracode |
| 8.1.9 | `app/cli.py` | 命令行入口 | ultracode |
| 8.1.10 | `app/tasks.py` | 双线程池调度 + 任务幂等去重 + 超时控制 + **跨平台单机文件锁（防重叠执行）**（详见 max 复核） | ultracode 初稿 → max 终审 |
| 8.1.11 | `app/messaging.py` | QQ 消息处理 | ultracode |
| 8.1.12 | `app/events.py` | 事件日志 | ultracode |
| **8.1.13** | **`app/routes/` 全局 404 handler** | FastAPI exception_handler → `Result(ok=False, error_code="APP_NOT_FOUND")`，所有 API 路径引用 `core.constants`（详见 max 复核） | ultracode 初稿 → max 终审 |
| **8.1.14** | **`templates/route_template.py`** | 路由模板 | ultracode |

### 8.2 Ops 层（ultracode）

| 步骤 | 文件 | 内容 | 档位 |
|------|------|------|------|
| 8.2.1 | `ops/__init__.py` | 新建 | ultracode |
| 8.2.2 | `ops/daemon.py` | 运维守护，HTTP 调 app，不 import 业务模块，引入 `install_signal_handlers()` | ultracode |
| 8.2.3 | `ops/guard.py` | 进程守护 | ultracode |

### 8.2-bis Lint 规则增强（xhigh）

| 步骤 | 内容 |
|------|------|
| 8.2.4 | `lint_imports.py` 追加规则 8（禁止顶层 IO/初始化：模块顶部不得做 DB 连接、模型加载、线程创建） |
| 8.2.5 | `lint_imports.py` 追加规则 9（DTO 冻结约束：`Result`/`MemoryItem`/`PromptData` 必须 `@dataclass(frozen=True)`） |
| 8.2.6 | `lint_imports.py` 追加规则 10（版本号必含：项目根 `version.py`，日志/健康接口输出版本号） |

> ⏸️ **Phase 8.1-8.2 完成检查点**：14 个 app 文件 + 3 个 ops 文件 + 3 条 lint 规则全部产出。

---

### 8.3 max 深度复核（逐个开启）

> 🔔 **切换档位**：`/effort max` — 原因：双线程池/生命周期/中间件/404 兜底 四个高风险节点需 max 复核（此阶段决定系统稳定性）

| 步骤 | 文件 | max 复核要点 |
|------|------|-------------|
| **8.3.1** | `app/tasks.py` | 双线程池：核心实时池（心跳/tick/欲望，低耗时高频率）+ 后台维护池（演化/摘要/重建，高耗时低频率）；任务幂等去重：`run_task_if_needed(task_id, window, fn)` 同一窗口只执行一次，服务重启不重复；超时控制：每个任务有独立超时；单任务失败不阻塞其他任务；**文件锁：跨平台单机锁，已运行则跳过，锁文件超时自动释放，锁状态纳入健康检查** |

> 🔔 **8.3.1 完成** → 仍在 max，继续 8.3.2（无需切换）

| **8.3.2** | `app/server.py` lifespan | 优雅启停逆序编排：先停上层任务→再停底层写入；`LifecycleComponent.stop(timeout)` 调用顺序正确（最大等待 10s，超时强制退出）；全局信号处理：SIGINT/SIGTERM/Ctrl+C → 触发优雅停止；uvicorn Windows 适配：`workers=1` + `ProactorEventLoop`；引入 `install_signal_handlers()` |

> 🔔 **8.3.2 完成** → 仍在 max，继续 8.3.3（无需切换）

| **8.3.3** | `app/middleware.py` | trace_id 跨线程/协程透传：`contextvars` 正确使用；QQ Bot 鉴权逻辑完整（从 auth.py 并入无遗漏）；接口限流规则正确（规则从 `core/config` 热更组读取）；限流触发 → `PLG_RATE_LIMITED` |

> 🔔 **8.3.3 完成** → 仍在 max，继续 8.3.4（无需切换）

| **8.3.4** | `app/routes/` 404 兜底 + 全局超时收口 | 全局 404 handler 返回 `Result(ok=False, error_code="APP_NOT_FOUND")`；所有路由路径引用 `core.constants`，无硬编码 URL；**全局超时收口验证**：LLM/嵌入/DB/SerpApi/图片 所有超时值均从 `core/config` 热更组读取，异常统一抛 `INF_TIMEOUT` |

> ⏸️ **Phase 8.3 完成检查点**：线程池/生命周期/中间件/404兜底 + 超时收口复核全部通过。

---

### 8.4 验证 + 测试（ultracode）

> 🔔 **切换档位**：`/effort ultracode` — 原因：max 复核完成，切回 ultracode 跑全功能验证

| 步骤 | 验证项 | 标准 |
|------|--------|------|
| 8.4.1 | 全功能 | 所有 API 端点正常响应 |
| 8.4.2 | 健康检查 | `GET /health` 返回 liveness + readiness + 健康评分 + 进程内存指标 + 文件锁状态 |
| 8.4.3 | 优雅启停 | Ctrl+C → 写入队列排空 → 所有连接关闭 → 退出，无数据丢失 |
| 8.4.4 | 任务幂等 + 文件锁 | 同一任务在窗口内不重复执行；文件锁防重叠生效 |
| 8.4.5 | 404 兜底 | 任意未定义路由 → `{"ok": false, "error_code": "APP_NOT_FOUND"}` |
| 8.4.6 | 接口限流 | 超频请求 → `PLG_RATE_LIMITED` |
| **8.4.7** | **单元测试** | `server.py`：①启动 ②lifespan 顺序 ③优雅停止 ④信号触发 |
| **8.4.8** | **单元测试** | `middleware.py`：①trace_id 注入 ②鉴权通过 ③鉴权失败 ④限流触发 |
| **8.4.9** | **单元测试** | `tasks.py`：①任务执行 ②幂等去重 ③文件锁抢占 ④超时控制 ⑤单任务失败不阻塞 |
| **8.4.10** | **单元测试** | `health.py`：①正常 ②degraded ③error ④10s TTL 缓存 |

### Phase 8 回滚条件
- 任一 API 端点 500
- 优雅停止时数据丢失
- 健康检查持续 error
- 回滚方式：保留旧 `main.py`，全量切回

---

## Phase 9: Cleanup（清理旧代码 + Lint 全规则通过）

**主力档位：high**

> 🔔 **切换档位**：`/effort high` — 原因：纯清理操作，无新代码产出，high 即可

### 9.1 清理步骤（high）

| 步骤 | 操作 | 档位 |
|------|------|------|
| 9.1.1 | 删除 `memory_engine/` 目录（已全部迁移） | high |
| 9.1.2 | 删除 `src/` 目录（已全部迁移） | high |
| 9.1.3 | 删除 `embedding/` 目录（已迁移到 `infra/embedding/`） | high |
| 9.1.4 | 删除旧转发层代码（Phase 2 添加的临时转发） | high |
| 9.1.5 | 更新 `main.py` → 改为从 `app.server` 导入 | high |
| 9.1.6 | 删除旧 `prompt/builder.py`（已迁移到新位置） | high |

### 9.2 验证（high）

| 步骤 | 验证项 | 标准 |
|------|--------|------|
| 9.2.1 | `lint_imports.py` 全规则 | **10 条规则全部通过**：层级依赖/私有包保护/循环依赖/`__init__.py` 禁止导出实现类/禁止运行期 resolve/禁止裸异常捕获/禁止硬编码 URL/禁止顶层 IO/DTO 冻结约束/版本号必含 + 路径大小写检查 |
| 9.2.2 | mypy 类型检查 | 跨层调用参数/返回值类型匹配，0 error |
| 9.2.3 | 无残留跨层引用 | grep 旧 import 路径无结果 |
| 9.2.4 | 旧代码观察 7 天 | 无异常后执行物理删除 |
| 9.2.5 | 全链路端到端 | 启动→消息→响应→记忆写入→检索，全链路正常 |

### Phase 9 回滚条件
- lint_imports 不通过
- 端到端测试失败
- 回滚方式：git revert

---

## Phase 10: 运行时防护补齐（第二梯队收尾）

**主力档位：ultracode（实现）+ max（复核）**

> 🔔 **切换档位**：`/effort ultracode` — 原因：实现离线缓存降级、日志清理、影子流量完善

### 10.1 离线缓存降级（ultracode）

| 步骤 | 文件 | 内容 | 档位 |
|------|------|------|------|
| 10.1.1 | `infra/llm_client/fallback_cache.py` | LLM/嵌入全部不可用时，返回本地高频问答缓存（TTL+热度淘汰） | ultracode |
| 10.1.2 | `app/routes/health.py` 增强 | 离线缓存命中率指标纳入健康检查 | ultracode |

### 10.2 运行时防护完善（ultracode）

| 步骤 | 内容 | 档位 |
|------|------|------|
| 10.2.1 | 日志清理定时任务 | `core/logging.py` 增加按日期切割 + 保留 7 天自动清理 + 错误日志永久归档 | ultracode |
| 10.2.2 | 影子流量隔离完善 | 支持按用户/账号灰度（不止全量百分比）；影子流量独立日志标记 + 独立指标统计 | ultracode |
| 10.2.3 | pre-commit hook | `.pre-commit-config.yaml`：提交前自动跑 `lint_imports.py` | ultracode |

### 10.3 max 深度复核（逐个开启）

> 🔔 **切换档位**：`/effort max` — 原因：离线缓存 + 影子流量 两个节点需最终安全复核

| 步骤 | 文件 | max 复核要点 |
|------|------|-------------|
| **10.3.1** | `infra/llm_client/fallback_cache.py` | 缓存 TTL 合理（避免过期数据误导用户）；热度淘汰正确；与主缓存隔离（不污染正常缓存）；缓存命中/未命中均打指标 |
| **10.3.2** | 影子流量 + 限流全链路 | 影子模式：按用户灰度正确隔离；影子流量不影响实际响应；限流规则在热更组；限流→`PLG_RATE_LIMITED`→健康检查不降级（限流是保护措施不是故障） |

### 10.4 验证（ultracode）

> 🔔 **切换档位**：`/effort ultracode` — 跑最终全链路验证

| 步骤 | 验证项 | 标准 |
|------|--------|------|
| 10.4.1 | 离线缓存 | 断网 → LLM 不可用 → 返回缓存兜底，非纯报错 |
| 10.4.2 | 日志清理 | 7 天前日志自动删除，错误日志保留 |
| 10.4.3 | pre-commit | `git commit` 自动触发 lint_imports |
| 10.4.4 | 全链路端到端（最终） | 所有功能正常，健康评分 > 90 |

---

## Phase 11: 后续迭代（第三/四梯队备忘）

> 以下内容在重构完成后按需推进，不纳入本次执行。

| 梯队 | 内容 | 说明 |
|------|------|------|
| 🟡 第三梯队 | 人格持久化（`data/personality_state.json`） | Phase 5 已实现基础，可增强 |
| 🟡 第三梯队 | 版本自迁移（`_MIGRATORS` 扩展） | Phase 3 已实现基础 |
| 🟡 第三梯队 | 多环境配置（dev/staging/prod） | `core/config.py` 支持 |
| 🟡 第三梯队 | Lint 规则升级（pre-commit + CI 集成） | Phase 10 已有 pre-commit |
| 🟢 第四梯队 | 交互式 CLI | `app/cli.py` 增强 |
| 🟢 第四梯队 | 开发热加载 | uvicorn --reload |
| 🟢 第四梯队 | 线程池监控 | `core/metrics.py` 扩展 |
| 🟢 第四梯队 | 脚手架自注册 | `gen_*.py` 增强为自动注册 |

---

## 附录 A：档位切换速查卡

| 场景 | 档位 | 约束 |
|------|------|------|
| 批量生成同质文件（如 6 个 features、10 个 plugins） | ultracode | 并行 agent，统一模板 |
| 单文件新模块骨架 | xhigh | 一次性产出完整骨架 |
| 核心契约终审（types/exceptions/container/signal_handler） | max | 单文件单次，附带铁律约束 |
| 并发安全复核（锁/队列/线程池） | max | 单逻辑点单次，只复核不发散 |
| 算法正确性复核（融合排序/拓扑调度/降级熔断） | max | 单逻辑点单次 |
| 批量删除/清理 | high | 低风险操作 |
| 验证/测试 | 跟随当前 phase 主力档位 | — |

## 附录 B：max 复核模板

每次开 max 时，prompt 前置：

```
【max 复核模式】
- 只复核当前文件：[文件路径]
- 复核维度：[契约正确性 / 并发安全 / 算法逻辑 / 异常边界 / 安全加固]
- 铁律：编码铁律 22 条全适用
- 禁止：过度思考、发散改其他文件、重构与复核无关的代码
- 产出：问题列表 + 修复建议，不直接改代码（等确认）
```

## 附录 C：回滚决策树

```
错误率 > 5%？
  ├─ 是 → 特性开关切 0%，秒级回滚
  └─ 否 → prompt 差异率 > 10%？
           ├─ 是 → 特性开关切 0%
           └─ 否 → 延迟 > 2x？
                    ├─ 是 → 特性开关切 0%
                    └─ 否 → 继续灰度
```

## 附录 D：lint_imports.py 规则启用时间表

| 规则 | 说明 | 启用 Phase |
|------|------|-----------|
| 1. 层级依赖 | 禁止上层 import 下层子模块 | Phase 1 |
| 2. 私有包保护 | 禁止外部 import `_` 前缀子包 | Phase 4 |
| 3. 循环依赖检测 | 全项目循环 import 检测 | Phase 1 |
| 4. `__init__.py` 导出 | 禁止导出实现类 | Phase 5 |
| 5. 禁止运行期 resolve | `DependencyContainer.resolve()` | Phase 6 |
| 6. 禁止裸异常捕获 | `except:` 视为违规 | Phase 7 |
| 7. 禁止硬编码 URL | API 路径必须引用 `core.constants` | Phase 7 |
| 8. 禁止顶层 IO/初始化 | 模块顶部不得做 DB 连接/模型加载/线程创建 | Phase 8 |
| 9. DTO 冻结约束 | `Result`/`MemoryItem`/`PromptData` `@dataclass(frozen=True)` | Phase 8 |
| 10. 版本号必含 | `version.py` + 日志/健康输出版本号 | Phase 8 |
| + 路径大小写检查 | Windows↔Linux 兼容 | Phase 1 |
| + mypy 类型检查 | 跨层调用类型匹配 | Phase 9 |

## 附录 E：单元测试覆盖清单

| Phase | 测试文件 | 覆盖接口 |
|-------|---------|---------|
| 1 | `core/types.py` | `Result[T]` 构造、错误、frozen |
| 1 | `core/exceptions.py` | 抛异常含 context、`__str__`、异常链 |
| 1 | `core/cache.py` | 读写、TTL、容量、空值缓存 |
| 2 | `infra/llm_client/client.py` | 正常、超时降级、全不可用兜底、连续熔断 |
| 2 | `infra/database/connection.py` | 连接、WAL 降级、连接池耗尽、迁移 |
| 2 | `infra/embedding/embedder.py` | 编码、空文本、未加载异常 |
| 3 | `memory/_indexes/vector.py` | add+search、并发安全、空索引、rebuild |
| 3 | `memory/_indexes/fusion.py` | 融合、单索引、空结果、权重 |
| 3 | `memory/_stores/resource.py` | 版本迁移、字段完整性、旧版本读取 |
| 4 | `memory/_core/write.py` | 写入、关闭排空、死信归档、重试、幂等 |
| 4 | `memory/_core/retrieve.py` | 检索、空结果、分级保护、超时 |
| 4 | `memory/_core/evolve.py` | 压缩一致性、触发边界、接口兼容 |
| 5 | `personality/state.py` | tick、快照恢复、注入规范、线程安全 |
| 5 | `personality/desire.py` | 生成、衰减、满足、边界 |
| 5 | `personality/life_stream.py` | 触发、持续、结束 |
| 6 | `domain/registry.py` | 注册、重名、循环依赖、自动发现 |
| 6 | `domain/tools/executor.py` | 执行、参数非法、超时、异常不泄露 |
| 7 | `plugins/registry.py` | 注册执行、重名、循环依赖、校验、异常不泄露 |
| 7 | 10 个插件 | 正常→Result(true)、异常→Result(false)、不抛异常 |
| 7 | `prompt/builder.py` | 渲染、空数据、extra、热加载 |
| 8 | `app/server.py` | 启动、lifespan、优雅停止、信号 |
| 8 | `app/middleware.py` | trace_id、鉴权通过/失败、限流 |
| 8 | `app/tasks.py` | 执行、幂等、文件锁、超时、不阻塞 |
| 8 | `app/routes/health.py` | 正常、degraded、error、TTL |
