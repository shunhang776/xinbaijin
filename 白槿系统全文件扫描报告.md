# 白槿 v2 系统全文件扫描报告

> 扫描时间：2026-06-15  
> 扫描范围：D:\xinbaijn 全部 68 个源文件（不含 `__pycache__`、`.git`、`backup/`、`data/`、`logs/`）

---

## 一、项目总览

白槿 v2 是一个纯 Windows 原生的 AI 数字生命系统。DeepSeek API 做 LLM 推理，BGE-M3 (1024维) + FAISS HNSW + BM25 做 100% 本地记忆，FastAPI 做 QQ Bot Webhook 服务端。

**五层架构（自下而上）：**

```
资源层 → 时间索引层 → 记忆单元层 → 类别层 → 插件层
```

---

## 二、完整目录树

```
D:\xinbaijn\
├── main.py                          # FastAPI 入口，QQ Bot Webhook + 全组件编排
├── cli.py                           # 轻量 CLI 客户端（HTTP 调用 main.py）
├── guard.py                         # Cron 进程守护（保 v2_ops.py 不死）
├── v2_ops.py                        # 运维守护（健康检查 + 自动重启 + 备份）
├── _launcher.py                     # 冷启动性能分析工具
├── persona.txt                      # 白槿人格自述（138行）
├── requirements.txt                 # pip 依赖清单
├── CLAUDE.md                        # Claude Code 融合指令
├── README.md                        # 项目说明
├── .env                             # 运行时环境变量
├── .env.template                    # 环境变量模板
│
├── config/
│   ├── config.yaml                  # 主配置（记忆/DeepSeek/服务器/运维）
│   └── identity.txt                 # 身份 + 沟通铁律（QQ 端用）
│
├── src/                             # 业务工具层（14 个模块）
│   ├── deepseek.py                  # DeepSeek API 客户端（流式+工具调用）
│   ├── tools.py                     # LLM function calling 工具定义
│   ├── memory.py                    # 记忆模块门面（兼容旧 Mem0 API）
│   ├── compressor.py                # 记忆压缩（LLM 摘要+删旧）
│   ├── internet.py                  # 联网搜索（SerpApi + 安全过滤）
│   ├── safety.py                    # 四层安全过滤器
│   ├── identity.py                  # 身份文件管理（加载/重载）
│   ├── activity.py                  # 用户活跃时段学习
│   ├── habits.py                    # 长期习惯检测
│   ├── easter_eggs.py               # 渐进式彩蛋回复
│   ├── vision.py                    # 图片理解（GPT-4o vision API）
│   ├── auth.py                      # QQ Bot ed25519 签名验证
│   ├── errors.py                    # 全局异常处理器
│   └── health.py                    # /health 健康检查端点
│
├── memory_engine/                   # 记忆引擎（31 个模块）
│   ├── __init__.py                  # 公共 API（get_engine / shutdown_engine）
│   ├── engine.py                    # 引擎入口（12步启动，统一 search/write）
│   ├── config.py                    # 全局配置（路径/FAISS/BM25/过期策略）
│   ├── constants.py                 # 业务常量（情绪/提醒/主动行为）
│   ├── utils.py                     # 工具函数（北京时间戳/格式化/校验）
│   ├── db.py                        # SQLite WAL 持久层
│   ├── state.py                     # 状态呼吸（情绪节律/第三意识体）
│   ├── senses.py                    # 具身感官采集器（身体/天气/电源/摄像头）
│   ├── desire_system.py             # 欲望系统（5大欲望主动驱动）
│   ├── life_stream.py               # 平行生活流（24h独立生活+活动记忆）
│   ├── idle_thoughts.py             # 内在独白（三层记忆漏斗）
│   ├── common_sense.py              # 社交直觉（FAISS+LLM双路/自生长）
│   ├── background.py                # 记忆背景化（潜意识语义云）
│   ├── write_pipeline.py            # 写入流水线（异步队列+偏好提取+情绪落盘）
│   ├── retrieve.py                  # 记忆检索+格式化（衰减/模糊化）
│   ├── retrieve_core.py             # 四路并行检索引擎核心
│   ├── index_vector.py              # FAISS HNSW 向量索引
│   ├── index_bm25.py                # BM25 全文索引（2-3 gram 中文分词）
│   ├── index_associative.py         # 关联索引（关键词→ID 双向映射）
│   ├── index_time.py                # 时间索引（bisect + 自然语言时间解析）
│   ├── category_store.py            # 类别层（Markdown 语义知识库）
│   ├── resource_store.py            # 资源层（JSONL 原始对话存储）
│   ├── evolve.py                    # 演化引擎（定时清理/升级/降级/去重/重建）
│   ├── cooccurrence.py              # 共现图（赫布可塑性/链式联想）
│   ├── preferences.py               # 偏好提取（规则驱动+去重）
│   ├── emotion.py                   # 情绪快照（A级永久记忆）
│   ├── memory_fidelity.py           # 艾宾浩斯遗忘曲线模型
│   ├── imperfect_speech.py          # 不完美表达（口吃/省略/重复）
│   ├── recent_chat.py               # 终端短期对话缓存
│   └── reminder.py                  # 提醒管理（A级持久化）
│
├── embedding/                       # 嵌入模型（3 个模块）
│   ├── __init__.py                  # 预热入口（preload / is_ready）
│   ├── embedder.py                  # BGE-M3 加载器（读写锁分离）
│   └── config_embed.py              # 模型路径/参数配置
│
├── plugins/                         # 插件层（1 个模块）
│   └── __init__.py                  # 门面导入（6个插件透传底层函数）
│
├── prompt/
│   └── builder.py                   # 零规则纯上下文系统提示构建器
│
├── tools/                           # 维护/评估工具
│   ├── rebuild_faiss.py             # FAISS 全量重建
│   ├── seed_common_sense.py         # 社交直觉种子填充
│   └── eval_common_sense.py         # 社交直觉 LLM 评估
│
├── scripts/
│   └── migrate_from_mem0.py         # Mem0 → memory_engine 迁移
│
├── docs/                            # 设计文档（9 个文件）
│   ├── AI_NATIVE_EXECUTION.md
│   ├── AI_NATIVE_PLAN.md
│   ├── BAIJIN_FINAL_REPORT.md
│   ├── BAIJIN_SYSTEM_GUIDE.md
│   ├── CODING_RULES.md
│   ├── MEMORY_ENGINE_SUMMARY.md
│   ├── SYSTEM_SUMMARY.md
│   ├── ai-native-转型草案.md
│   ├── emotion-system.md
│   ├── memory-backgrounding-design.md
│   ├── sensory-snapshot-design.md
│   └── 白槿系统现状总结报告_v3.0.docx
│
└── test files/
    ├── test_bp.py
    ├── test_minimal.py
    ├── test_server.py
    └── test_sp.py
```

---

## 三、进程守护三层架构

```
Cron (每10分钟)
  └─ guard.py        检查 v2_ops.py 是否存活，死了就拉起
       └─ v2_ops.py  持续巡检：端口可达 → /health 检查 → 条件重启
            └─ main.py   FastAPI 服务（核心 AI 逻辑）
                 └─ cli.py  轻量 HTTP 客户端（查询用，不常驻）
```

| 组件 | 角色 | 依赖 | 被谁依赖 |
|------|------|------|----------|
| `guard.py` | 最外层保镖 | wmic 命令 | Cron |
| `v2_ops.py` | 运维守护 | config.yaml, netstat, taskkill | guard.py |
| `main.py` | 核心服务（8080端口） | 全部 memory_engine/src/embedding | v2_ops.py |
| `cli.py` | 轻量查询客户端 | 标准库 urllib | 用户/脚本 |
| `_launcher.py` | 启动耗时分析 | memory_engine.utils, embedding | 开发者 |

---

## 四、src/ 业务工具层（14 个模块）

### 4.1 deepseek.py — LLM API 客户端

**职责：** DeepSeek API 的完整封装。持久 httpx 连接池、指数退避重试（3次）、流式/非流式/工具调用三种接口、详细错误码处理（429/401）。

**核心函数：**
| 函数 | 签名 | 说明 |
|------|------|------|
| `generate` | `(messages, model, max_tokens, temperature) -> str` | 流式生成，3次重试 |
| `generate_with_tools` | `(messages, tools, executor, model, max_turns) -> str` | 异步工具调用循环 |
| `generate_with_tools_sync` | `(messages, tools, executor, model, max_turns, timeout) -> str` | 同步工具调用循环 |
| `_stream_collect` | 内部 | 流式 token 收集 |
| `_parse_tool_calls` | 内部 | 兼容 DeepSeek/OpenAI 格式的工具调用解析 |

**被依赖：** compressor.py, health.py, main.py, desire_system.py, idle_thoughts.py

---

### 4.2 tools.py — LLM Function Calling 工具

**职责：** 定义 6 个 LLM 可调用工具（OpenAI function-calling 格式），并提供统一的 execute 分发器。

**6 个工具：**
| 工具名 | 功能 | 底层实现 |
|--------|------|----------|
| `recall_memory` | 语义搜索记忆 | src/memory.py → memory_engine |
| `remember` | 写入新记忆 | src/memory.py → memory_engine |
| `forget_memory` | 删除记忆（A/S级保护） | src/memory.py → memory_engine |
| `update_identity_rule` | 修改沟通铁律 | src/identity.py |
| `set_reminder` | 设置提醒 | memory_engine/reminder.py |
| `search_internet` | 联网搜索 | src/internet.py |

**被依赖：** main.py, plugins/__init__.py

---

### 4.3 memory.py — 记忆模块门面

**职责：** 兼容旧 Mem0 API 的门面层，全部委托给自研 memory_engine。

| 函数 | 说明 |
|------|------|
| `store(text, metadata)` | 写入记忆，自动检测对话格式 |
| `search(query, top_k)` | 语义搜索，兼容旧 Mem0 返回格式 |
| `recall(query)` | 搜索+格式化为字符串 |
| `delete(memory_id)` | 软删除（A/S级保护） |
| `count()` | 记忆总数 |
| `is_ready()` | 引擎是否就绪 |

**被依赖：** tools.py, main.py, migrate_from_mem0.py

---

### 4.4 compressor.py — 记忆压缩

**职责：** 用 DeepSeek 将多条旧记忆总结为一条摘要，删旧存新。

| 函数 | 说明 |
|------|------|
| `compress(store_fn, search_fn, delete_fn)` | 扫描>3天记忆，>=3条时压缩 |

**被依赖：** main.py

---

### 4.5 internet.py — 联网搜索

**职责：** SerpApi Google 搜索 → 四层安全过滤 → 第一人称情感改写。

| 函数 | 说明 |
|------|------|
| `search(query, limit)` | 关键词搜索 |
| `browse(category, limit)` | 按分类随机搜索（news/fun/knowledge） |
| `is_available()` | SERPER_API_KEY 是否已配置 |

**被依赖：** tools.py, plugins/__init__.py, main.py

---

### 4.6 safety.py — 安全过滤器

**职责：** 四层过滤——关键词黑名单 → 域名白名单 → 敏感正则 → 截断。

| 函数 | 说明 |
|------|------|
| `filter_result(result)` | 全流程过滤，不安全返回 None |
| `is_safe(result)` | 便捷布尔判断 |

**被依赖：** internet.py, plugins/__init__.py

---

### 4.7 identity.py — 身份管理

**职责：** 线程安全的身份文件加载/获取/重载。LLM 可通过工具自我修改沟通铁律。

| 函数 | 说明 |
|------|------|
| `load()` | 惰性加载 identity.txt |
| `get_identity()` | 获取身份文本 |
| `reload()` | 强制重读文件 |

**被依赖：** tools.py, plugins/__init__.py, main.py

---

### 4.8 activity.py — 活跃时段学习

**职责：** 按小时桶记录用户交互，学习活跃窗口，决策是否主动发消息。

| 函数 | 说明 |
|------|------|
| `record()` | 记录一次交互 |
| `get_windows()` | 活跃时段（小时桶>=50%均值） |
| `is_active_now()` | 当前是否在活跃窗口 |
| `hours_since_last()` | 距上次交互的小时数 |
| `should_greet()` | 是否应该主动问候 |

**被依赖：** main.py, memory_engine/state.py

---

### 4.9 habits.py — 习惯检测

**职责：** 从记忆中用 LLM 识别用户长期习惯，按星期/日期匹配。

| 函数 | 说明 |
|------|------|
| `match_today()` | 返回今天的匹配习惯 |
| `_build_habit_prompt(texts)` | 构建 LLM 习惯识别 prompt |
| `_parse_habits(result)` | 解析 LLM 结构化响应 |

**被依赖：** main.py

---

### 4.10 easter_eggs.py — 彩蛋

**职责：** 触发词 + 渐进式回复列表。同一词连续触发推进序列，换词重置。

| 函数 | 说明 |
|------|------|
| `match(text)` | 匹配触发词，返回下一个渐进回复 |

**被依赖：** main.py

---

### 4.11 vision.py — 图片理解

**职责：** 下载 QQ 消息中的图片 → base64 → GPT-4o vision API → 文字描述。含 MIME 检测和 10MB 限制。

| 函数 | 说明 |
|------|------|
| `describe(url, prompt)` | 图片→文字描述 |
| `extract_urls(event_data)` | 从 QQ 事件提取图片/sticker URL |
| `extract_emoji_text(content)` | CQ:face 标记→中文描述 |
| `is_available()` | Vision API 是否可用 |

**被依赖：** main.py

---

### 4.12 auth.py — QQ Bot 认证

**职责：** ed25519 签名验证（op=13 回调 + Webhook）。SHA256 降级兜底。

| 函数 | 说明 |
|------|------|
| `op13_sign(plain_token, event_ts)` | op=13 回调签名 |
| `verify(body_bytes, sig, event_ts)` | Webhook 签名验证 |

**被依赖：** main.py

---

### 4.13 errors.py — 全局异常处理

**职责：** 确保所有异常返回 QQ Bot ACK `{"op": 12}`，防止平台重发。含用户降级回复。

| 函数 | 说明 |
|------|------|
| `global_exception_handler` | 未处理异常→ACK+降级回复 |
| `http_exception_handler` | HTTPException→ACK |
| `json_parse_handler` | JSON 解析失败→ACK |
| `register_handlers(app)` | 注册到 FastAPI |

**被依赖：** main.py

---

### 4.14 health.py — 健康检查

**职责：** `/health` 端点，检查 DeepSeek/QQ API/模块就绪状态。

| 函数 | 说明 |
|------|------|
| `register_health_route(app)` | 注册 GET /health |
| `module_status()` | embedding/memory/identity 就绪状态 |
| `resource_usage()` | CPU/运行时长/调试模式 |

**被依赖：** main.py

---

## 五、memory_engine/ 记忆引擎（31 个模块）

### 5.1 跨层基础设施

#### config.py — 全局配置

所有常量、路径、可调参数的唯一定义位置。零硬编码，同一值只出现在一个地方。

**关键配置组：**
- 路径：`DB_PATH`, `FAISS_INDEX_PATH`, `CATEGORIES_DIR`, `RESOURCES_DIR`, `BACKUP_DIR`
- FAISS：`VECTOR_DIM=1024`, `HNSW_M=32`, `EF_CONSTRUCTION=200`, `EF_SEARCH=64`
- BM25：`BM25_K1=1.5`, `BM25_B=0.75`
- 检索：`TOP_K=20`, `BM25_WEIGHT=0.3`, `VECTOR_WEIGHT=0.7`
- 过期：`C_GRADE_EXPIRE_DAYS=7`, `HOT_DATA_DAYS=180`
- 情绪：`EMOTION_TIME_WINDOW_DAYS=30`, `EMOTION_TYPE_WEIGHTS`, `EMOTION_TYPE_DECAY`
- 欲望：`DESIRE_MAX_DELTA_SEC`, `DESIRE_GLOBAL_MAX_OUT_MSG_PER_MONTH=2`
- 社交直觉种子：29 条预置场景-直觉映射

**被依赖：** 几乎全部 25+ 模块

#### constants.py — 业务常量

与 config.py 分离，存放业务逻辑常量而非路径/参数。

- 情绪：`EMOTION_SAVE_MIN_INTERVAL=300`（5分钟去重）
- 提醒：`REMINDER_AHEAD_SECONDS=86400`（提前1天）
- 主动行为：`ACTIVE_MAX_PER_DAY=2`, `ACTIVE_BASE_PROBABILITY=0.25`

**被依赖：** emotion.py, reminder.py

#### utils.py — 工具函数

北京时间戳、格式化、校验的统一入口。

| 函数 | 说明 |
|------|------|
| `now_ts() -> int` | 北京时间秒级时间戳 |
| `ts_to_datetime(ts)` | 戳→datetime |
| `fmt_ts(ts)` | 戳→ISO 字符串 |
| `day_str(ts)` | 戳→'YYYY-MM-DD' |
| `week_str(ts)` | 戳→'YYYY-W##' |

**被依赖：** 10+ 模块

---

### 5.2 资源层（最底层持久化）

#### db.py — SQLite WAL 持久层

**职责：** 写操作全事务化，每线程独立连接，增量迁移系统，访问计数队列批量落盘。

**核心类：** `Database`

| 方法 | 说明 |
|------|------|
| `insert_item(item)` | 插入/替换记忆 |
| `soft_delete(iid)` | 软删除（A/S级抛出 PermissionError） |
| `insert_fact(fact)` | 插入事实 |
| `get_item(iid)` | 获取记忆（自动排队更新访问计数） |
| `query(sql, params)` | 通用查询 |
| `load_all_items()` | 分页加载所有未删除记忆 |
| `load_all_facts()` | 加载所有事实 |
| `save_id_mapping(mapping)` / `load_id_mapping()` | FAISS ID 映射持久化 |

**表结构：** `items`（记忆）, `facts`（会话事实）, `id_mapping`（FAISS映射）, `_migrations`（迁移记录）

**被依赖：** engine.py, write_pipeline.py, retrieve_core.py, retrieve.py, recent_chat.py, emotion.py, reminder.py, evolve.py, common_sense.py, state.py

#### resource_store.py — 资源层

**职责：** 原始对话按天写入 JSONL 文件。唯一不进内存的层。

**核心类：** `ResourceStore`

| 方法 | 说明 |
|------|------|
| `add(messages, timestamp)` | 追加一条对话到当日 JSONL |
| `read_day(date_str)` | 读取某天全部原始对话 |
| `read_recent_days(n_days)` | 读取最近 N 天原始对话（时间升序） |

**被依赖：** engine.py, write_pipeline.py, evolve.py

---

### 5.3 时间索引层

#### index_time.py — 时间索引

**职责：** UTC bisect 二分检索，全量内存常驻。支持 jionlp NER 自然语言时间解析 + 规则回退。

**核心类：** `TimeIndex`

| 方法 | 说明 |
|------|------|
| `load_all(items)` | 批量加载排序构建索引 |
| `search(query)` | 检索：规则→jionlp→bisect范围查询 |
| `add(item_id, timestamp)` | 插入时间记录 |
| `remove(item_id)` | 移除时间记录 |

**被依赖：** engine.py, write_pipeline.py, retrieve_core.py, evolve.py

---

### 5.4 记忆单元层

#### index_vector.py — FAISS HNSW 向量索引

**职责：** 1024 维 float32，强制单线程防 segfault。快照→构建→增量。

**核心类：** `VectorIndex`

| 方法 | 说明 |
|------|------|
| `load_snapshot(id_map)` | 从磁盘恢复 |
| `build(items)` | 首次全量构建 HNSW |
| `add(item_id, text)` | 单条追加（BGE编码） |
| `search(query, top_k, max_distance)` | 检索，返回 [(item_id, distance)] |
| `search_vec(q_vec, top_k, max_distance)` | 用预编码向量检索 |
| `remove(item_id)` | **仅删映射**（FAISS 不支持单条删除） |
| `rebuild(items)` | 全量重建（清理已删条目） |
| `save_snapshot()` | 写磁盘+返回 ID 映射 |

**注意：** IndexHNSWFlat 不支持 remove。`remove()` 只从 Python 映射中移除，FAISS 索引内向量不删。检索时通过 `iid in self._id_to_faiss_idx` 过滤，但会吃掉 top_k 名额。等月度 rebuild 彻底清理。

**被依赖：** engine.py, write_pipeline.py, retrieve_core.py, evolve.py

#### index_bm25.py — BM25 全文索引

**职责：** 2-3 gram 中文分词，倒排索引全量常驻内存。

**核心类：** `BM25Index`

| 方法 | 说明 |
|------|------|
| `add(item_id, text)` | 添加/更新文档 |
| `remove(item_id)` | 移除文档 |
| `search(query, top_k)` | BM25 评分排序检索 |
| `_tokenize(text)` | 2-3 gram 中文分词 |

**BM25 公式：** `idf * tf*(k1+1) / (tf + k1*(1-b + b*dl/avg_dl))`

**被依赖：** engine.py, write_pipeline.py, retrieve_core.py, evolve.py

#### index_associative.py — 关联索引

**职责：** 关键词→记忆ID 双向映射，dict 直查 <=1ms。

**核心类：** `AssociativeIndex`

| 方法 | 说明 |
|------|------|
| `add(item_id, keywords)` | 添加/更新关键词映射 |
| `remove(item_id)` | 移除映射 |
| `search(query, top_k)` | 关键词包含匹配 |
| `get_keywords(item_id)` | 获取文档关键词 |

**被依赖：** engine.py, write_pipeline.py, retrieve_core.py, evolve.py

#### retrieve_core.py — 四路并行检索引擎

**职责：** 时间索引 + BM25 + 向量搜索 + 关联索引，动态优先级合并，情绪关联。

**核心类：** `RetrieveEngine`

| 方法 | 说明 |
|------|------|
| `search_sync(query, query_vec)` | 同步检索入口（query_vec 主线程预编码） |
| `_merge(query, ...)` | 动态优先级合并去重，session事实权重×15 |
| `_get_emotion_timeline()` | 情绪时间线（60s TTL缓存） |
| `_associate_emotions(results)` | 时间+关键词双重过滤关联情绪 |

**辅助函数：**
- `classify_query(query)` — 根据查询内容决定四路优先级
- `_classify_emotion_type(content)` — 情绪文本→类型
- `_text_similarity(a, b)` — Jaccard 轻量文本相似度

**被依赖：** engine.py

#### retrieve.py — 记忆检索+格式化

**职责：** 艾宾浩斯衰减 + 概率遗忘 + 模糊化文本 + prompt 格式化。

| 函数 | 说明 |
|------|------|
| `get_relevant_memories(query, ...)` | 检索后衰减排序，A级100%返回 |
| `format_memory_for_prompt(memories, sender_type)` | 格式化为prompt（含emoji/情绪前缀/模糊化/保真度注释） |

**被依赖：** prompt/builder.py

#### memory_fidelity.py — 艾宾浩斯遗忘模型

**职责：** 模拟人类记忆衰减。A级永久记忆完全豁免。

| 函数 | 说明 |
|------|------|
| `get_fidelity(days)` | 天数→保真度 0-1（区间线性插值） |
| `should_remember(days)` | 概率性遗忘 |
| `blur_text(text, fidelity)` | 按保真度模糊（最多50%内容→"..."） |
| `fidelity_note(fidelity)` | 自然语言注释（"有点模糊"/"记得不太清楚了"） |

**衰减阈值：** 1天=1.0, 3天=0.9, 7天=0.7, 30天=0.4, 90天=0.15, 365天=0.05

**被依赖：** retrieve.py

#### write_pipeline.py — 写入流水线

**职责：** 同步写 Resource → 入队列 → 单线程后台处理索引写入。崩溃可恢复（全量快照）。

**核心类：** `WritePipeline`

| 方法 | 说明 |
|------|------|
| `write(user_msg, assistant_reply, ...)` | 同步写ResourceStore+入队列 |
| `_process_task(task)` | 偏好提取→分类/关键词/分级→写核心索引→情绪快照→共现图→隐式偏好 |
| `_classify(user_msg, reply)` | 分类：preference/commitment/event/knowledge/context |
| `_grade(user_msg, reply)` | 分级：A（承诺/重要关键词）、C（<30字）、B（默认） |
| `_extract_keywords(text)` | 2-4字中文 n-gram + Counter |
| `_add_cooccurrence_edges(item_id, keywords)` | 新记忆→关键词重叠记忆建赫布连接 |

**被依赖：** engine.py

#### emotion.py — 情绪快照

**职责：** 每次对话后保存白槿第一人称心情（A级永久记忆）。Claude Code 通过 `--emotion` 参数传入。

| 函数 | 说明 |
|------|------|
| `save_emotion_snapshot(db, user_msg, assistant_reply, cloud_snapshot, emotion_event)` | 保存情绪快照（5分钟去重） |
| `get_recent_emotions(db, days)` | 获取近期+早期情绪演变轨迹 |
| `cleanup_expired(db, archive_days)` | 物理清理过期情绪 |

**被依赖：** write_pipeline.py, state.py, retrieve_core.py

#### reminder.py — 提醒管理

**职责：** 待关心事件提醒。A级永久记忆。

| 函数 | 说明 |
|------|------|
| `save_reminder(db, content, trigger_time)` | 保存提醒（24h内去重） |
| `get_due_reminders(db)` | 获取到期提醒（提前1天查询） |
| `mark_reminder_done(db, rid)` | 标记完成 |

**被依赖：** tools.py, prompt/builder.py, main.py

#### cooccurrence.py — 共现图

**职责：** 赫布可塑性工程实现。无向加权图，链式联想激活传播，睡眠整合。

**核心类：** `CooccurrenceGraph`

| 方法 | 说明 |
|------|------|
| `add_connection(mem_a, mem_b, strength)` | 建边（双向加强，上限1.0） |
| `add_connections_batch(new_mem_id, active_ids, strength)` | 新记忆时批量建边 |
| `get_strength(mem_a, mem_b)` | 获取边强度（日衰减5%） |
| `get_neighbors(mem_id, min_strength)` | 获取有效邻居 |
| `propagate_activation(activations, boost_factor)` | 链式联想激活传播（单层） |
| `consolidate()` | 睡眠整合：修剪弱边（<0.05）、强化强边（>0.5 ×1.2） |
| `save(path)` / `load(path)` | pickle 持久化 |
| `remove_node(mem_id)` | 删除节点的所有连接 |

**被依赖：** engine.py, background.py, write_pipeline.py

#### background.py — 记忆背景化

**职责：** 每次对话自动产生潜意识语义云——不等用户提问。

**核心类：** `MemoryBackgrounder`

| 方法 | 说明 |
|------|------|
| `get_semantic_cloud(user_msg, recent_context, total_memories, query_vec)` | 主入口：候选→激活值→联想→归一化→抑制→语义云 |
| `_calc_raw_activations(candidates)` | 激活值公式：`a = s * exp(-k*hours) * (1+emotion_weight)` |
| `_competitive_normalize(activations)` | Softmax 归一化（温度0.5） |
| `_build_cloud(normalized, raw, candidates)` | 构建重复稀释语义云（≤15 token） |

**被依赖：** engine.py

#### evolve.py — 演化引擎

**职责：** 定时后台维护。过期清理/每日摘要/去重/升级/降级/归档/月度重建。

**核心类：** `EvolutionEngine`

| 方法 | 说明 |
|------|------|
| `cleanup_expired()` | 3:00 清理过期 C 级记忆 |
| `cleanup_resources()` | 删除过期 JSONL |
| `generate_daily_summary()` | 4:00 生成昨日摘要到类别层 |
| `backup()` | 备份 DB+FAISS |
| `light_maintenance()` | 每3天：高频记忆升级 |
| `full_maintenance()` | 每周日：升级+降级+去重 |
| `_promote_frequent_items()` | 高频访问→升级 |
| `_demote_stale_items()` | 长期未访问→降级 C |
| `_deduplicate()` | MinHash+LSH 去重（依赖 datasketch） |
| `monthly_health_check()` | 每月1日全量重建 FAISS |

**被依赖：** engine.py

#### recent_chat.py — 短期对话缓存

| 函数 | 说明 |
|------|------|
| `get_recent_chat(db, n)` | 获取最近 n 条对话 |
| `format_recent_chat(db, n)` | 格式化为 prompt 文本 |

**被依赖：** prompt/builder.py

#### imperfect_speech.py — 不完美表达

**职责：** 10%概率给短回复添加人类口癖（口吃/省略号/重复字）。

| 函数 | 说明 |
|------|------|
| `add_imperfections(text)` | 随机选一种瑕疵修饰 |

**被依赖：** main.py（回复后处理）

---

### 5.5 类别层

#### category_store.py — 类别层

**职责：** Markdown 文件全量内存常驻，自动同步磁盘修改。

**核心类：** `CategoryStore`

| 方法 | 说明 |
|------|------|
| `load_all()` | 启动时全量加载所有 .md 文件 |
| `read(rel_path)` | 读取（磁盘有更新自动刷新） |
| `read_relevant(query)` | 关键词匹配→Top3 拼接 |
| `write(rel_path, content)` | 写入+更新缓存 |
| `append(rel_path, entry)` | 追加一条 |

**被依赖：** engine.py, evolve.py, write_pipeline.py, retrieve_core.py

#### preferences.py — 偏好提取

**职责：** 规则驱动从对话中识别用户偏好，追加到偏好 Markdown 文件。

| 函数 | 说明 |
|------|------|
| `auto_extract_preference(user_msg)` | 触发词匹配+噪音过滤+去重 |
| `analyze_reaction(user_reply, previous_reply)` | 分析用户反应→隐式偏好 |
| `get_preferences()` | 读取最近30条偏好 |

**被依赖：** write_pipeline.py

---

### 5.6 插件层（上层意识模块）

#### __init__.py — 公共 API 入口

| 函数 | 说明 |
|------|------|
| `get_engine()` | 线程安全延迟初始化 MemoryEngine 单例 |
| `shutdown_engine()` | 安全关闭 |

#### engine.py — 引擎总入口

**核心类：** `MemoryEngine`

**12 步启动序列：**
1. 加载类别层
2. 加载时间轴
3. 加载 BM25
4. 加载关联索引
5. 加载 FAISS（快照或构建）
6. 启动写入线程
7. 启动演化引擎
8. 初始化感官采集器
9. 初始化状态呼吸
10. 初始化欲望系统
11. 初始化社交直觉（预热经验库）
12. 启动自检（已禁用）

| 关键方法 | 说明 |
|------|------|
| `search_sync(query)` | 同步检索 |
| `remember(user_msg, assistant_reply, ...)` | 写入对话（异步） |
| `remember_sync(...)` | 写入对话（同步） |
| `record_fact(content, ts)` | 写入会话事实（B级） |
| `record_impression(content, ts)` | 写入短期印象（C级，7天过期） |
| `shutdown()` | 安全关闭（停止线程+刷新+保存快照） |

**子组件：** Database, TimeIndex, VectorIndex, BM25Index, AssociativeIndex, CategoryStore, ResourceStore, CooccurrenceGraph, MemoryBackgrounder, RetrieveEngine, WritePipeline, EvolutionEngine

#### state.py — 状态呼吸

**职责：** 白槿的情绪节律系统。持续演化的心情/能量/社交欲/第三意识体/共时性。

**核心类：** `BreathingState`

| 方法 | 说明 |
|------|------|
| `tick()` | 每秒推进：昼夜节律/沉默加成/涟漪衰减/心情回归/不可判定状态 |
| `ripple(delta, event_type)` | 外部事件扰动（用户消息+0.03，负面记忆-0.05） |
| `describe()` | 自然语言状态描述（含三维走神模拟） |
| `get_third_text()` | 根据陪伴时长返回温情语句 |
| `get_reply_tone_modifier()` | 不可判定状态→回复语气修饰 |
| `calc_depth_index()` | 关系深度指数 |
| `check_coincidence()` | 共时性检测 |

**被依赖：** engine.py, desire_system.py, life_stream.py

#### senses.py — 具身感官采集器

**职责：** 给白槿一副"身体仪表盘"。三条路径：感官快照/触觉模拟/时间流逝感。

**核心类：** `SenseCollector`（强制单例）

| 方法 | 说明 |
|------|------|
| `snapshot(msg_count)` | 完整感官快照：时间/身体/电源/摄像头/天气/运行时长 |
| `format_prompt(snap)` | 格式化为 prompt 片段 |
| `_read_body()` | CPU/内存/温度 |
| `_format_body(body)` | 身体隐喻+精确数值 |
| `_sense_time()` | 日期+星期+光环境+时间体感+天气体感 |
| `_get_weather()` | 高德 API 天气（15分钟缓存） |
| `_sense_power()` | 电源状态（电池/插电/台式机） |

**被依赖：** engine.py

#### desire_system.py — 欲望系统

**职责：** 从"被动响应"到"主动意图"。时间差+每秒速率累积模型，抗 Tick 抖动/重启断层。

**五大欲望：**

| 欲望 | 阈值 | 冷却 | 速率 | 说明 |
|------|------|------|------|------|
| ReachOutDesire（联络欲） | 60 | 4h | 0.002/s | 独处越久越想找人 |
| ShareObservationDesire（分享欲） | 55 | 3h | 0.001/s | 感知新鲜事物+联想记忆 |
| LearnAboutDesire（求知欲） | 65 | 6h | 0.0008/s | 闲思触及深度+记忆缺口 |
| CreateSomethingDesire（创作欲） | 60 | 8h | 0.0005/s | 高精力+好心情+生活流素材 |
| ReflectOnDesire（反思欲） | 50 | 4h | 0.0015/s | 深夜/情绪低落/翻阅记忆 |

**核心类：** `DesirePool`

| 方法 | 说明 |
|------|------|
| `batch_accumulate()` | 批量累积强度 |
| `check_and_execute()` | 触发检查+执行（每月最多主动发2条消息） |

**被依赖：** engine.py

#### life_stream.py — 平行生活流

**职责：** 白槿有自己独立的 24 小时生活。习惯养成+活动记忆+自引用。

**核心类：** `LifeStreamEngine`

**15 种基础活动：** 翻书/听音乐/泡茶/整理桌面/望向窗外/摆弄绿植/练字/临摹/做手冲咖啡/看纪录片/整理旧照片/给植物浇水/翻旧笔记本/发呆/练琴

| 方法 | 说明 |
|------|------|
| `tick(current_mood)` | 活动结束感悟/主动分享 |
| `on_user_message(mood)` | 用户消息时响应（含活动打断逻辑） |
| `_start_new_activity(mood)` | 情绪联动加权+习惯加成选活动 |
| `_try_enrich_activity(name)` | 25%概率联网搜索丰富活动内容 |
| `get_past_reference()` | 10%概率自引用过去活动 |

**被依赖：** engine.py, desire_system.py

#### idle_thoughts.py — 内在独白

**职责：** 三层记忆漏斗。90%瞬间（内存）→ 9%短期（C级落盘）→ 1%长期（睡眠整合钩子）。

| 函数 | 说明 |
|------|------|
| `should_generate_idle_thought(...)` | 触发条件检查 |
| `generate_rule_based(...)` | 规则引擎生成念头 |
| `generate_llm_thought()` | DeepSeek 生成念头（日限2次，冷却30min） |
| `tick_idle_thoughts(engine, ...)` | 主入口 |

**被依赖：** main.py（调度）

#### common_sense.py — 社交直觉

**职责：** AI Native 常识感知模块。经验从对话中自动复盘沉淀，自生长闭环。

**核心函数：**

| 函数 | 说明 |
|------|------|
| `get_social_intuition(user_msg, recent_context, query_vec)` | 主接口：快速通道→经验匹配→LLM推理 |
| `reflect_on_reply(user_msg, reply, recent_context)` | 异步自检：对话后自动沉淀经验 |
| `add_experience(scene, intuition, note, confidence)` | 沉淀社交经验（自动去重/合并） |
| `encode_text(text)` | 公共文本编码接口 |
| `list_experiences()` | 列出所有社交经验 |

**被依赖：** engine.py, plugins/__init__.py

---

## 六、embedding/ 嵌入模型（3 个模块）

| 模块 | 职责 | 关键点 |
|------|------|--------|
| `config_embed.py` | 模型配置 | BGE-M3, 1024维, CPU环境, 无FP16 |
| `embedder.py` | 模型加载器 | `BGEEmbedder` 封装 FlagModel, 读写锁分离 |
| `__init__.py` | 预热入口 | `preload()` 启动时预热, `is_ready()` 健康检查 |

**加载链：** `config_embed.py` → `embedder.py` → `__init__.py`  
**预热耗时：** ~20s（391个权重分片 CPU 加载）  
**模型路径：** `C:/Users/l2038/.cache/huggingface/hub/BAAI/bge-m3`

---

## 七、plugins/ 插件层（1 个模块）

### plugins/__init__.py

**当前状态：** 门面（Facade）——从 `memory_engine` 和 `src` 导入底层函数并重新导出。不含编排逻辑。

**已落地的 6 个插件（底层函数透传）：**

| 插件名 | 导出符号 | 底层来源 |
|--------|----------|----------|
| tools | `TOOLS`, `run_tool` | `src/tools.py` |
| intuition | `get_social_intuition`, `reflect_on_reply`, `encode_text` | `memory_engine/common_sense.py` |
| emotion | `get_recent_emotions`, `save_emotion_snapshot` | `memory_engine/emotion.py` |
| remind | `get_due_reminders`, `save_reminder`, `mark_reminder_done` | `memory_engine/reminder.py` |
| search | `search_internet`, `filter_search_result` | `src/internet.py` + `src/safety.py` |
| identity | `load_identity`, `get_identity`, `reload_identity` | `src/identity.py` |

**未落地的 3 个插件（docstring 写了但无代码）：**

| 插件名 | 描述 |
|--------|------|
| recall | 四路召回编排 |
| remember | 异步写入流水线 |
| forget | A/S 级软删除保护 |

**注意：** `recall`/`remember`/`forget` 的底层功能在 `memory_engine` 中完全可用，但插件层缺少封装编排——如限流、保护规则校验、多路结果合并等逻辑。

---

## 八、prompt/ 提示构建（1 个模块）

### prompt/builder.py

**职责：** 零规则纯上下文系统提示构建器。不给规则，只给信息——让白槿自然决定如何回应。

**核心函数：** `build_prompt(db, user_msg, sender_type, query_vec) -> tuple[str, str]`

**组装部件：**

| 部件 | 来源 | 说明 |
|------|------|------|
| 语义云+候选记忆 | `background.py` | 相关记忆的自然浮现 |
| 感官快照 | `senses.py` | 天气/身体/时间感 |
| 生活流 | `life_stream.py` | 当前正在做的活动 |
| 内在独白 | `idle_thoughts.py` | 掠过的念头 |
| 界状态 | `state.py` + `emotion.py` | 情绪趋势+语气修饰 |
| 社交直觉 | `common_sense.py` | 社交情境感知 |
| 身份 | `persona.txt` | 白槿自我认知 |
| 偏好 | `preferences.py` | 用户偏好 |
| 提醒 | `reminder.py` | 到期提醒 |
| 短期对话 | `recent_chat.py` | 最近对话上下文 |

**被依赖：** main.py（回复前构建 prompt）

---

## 九、tools/ 维护工具（3 个）

| 工具 | 职责 | 接触的模块 |
|------|------|-----------|
| `rebuild_faiss.py` | FAISS 全量重建（备份→重建→原子替换） | VectorIndex, SQLite |
| `seed_common_sense.py` | 社交直觉经验库初始种子填充（5条） | common_sense.py |
| `eval_common_sense.py` | LLM 语义评估社交直觉质量（15个测试用例） | common_sense.py, DeepSeek API |

---

## 十、scripts/ 迁移脚本（1 个）

| 脚本 | 职责 |
|------|------|
| `migrate_from_mem0.py` | Mem0 → memory_engine 一次性迁移（导出→转换→写入全索引） |

---

## 十一、核心数据流

### 用户消息→回复 全链路

```
QQ Webhook (POST /)
  → main.py:_handle_message()
    → auth.verify()                                  签名验证
    → main.py:_process_message()
      → easter_eggs.match()                          彩蛋检测
      → embedding.embedder.encode_query()            查询编码
      → prompt.builder.build_prompt()                系统提示构建
        → background.get_semantic_cloud()            语义云
        → senses.snapshot()                          感官快照
        → state.describe()                           情绪状态
        → common_sense.get_social_intuition()        社交直觉
        → life_stream.on_user_message()              生活流
        → idle_thoughts.tick_idle_thoughts()         内在独白
        → preferences.get_preferences()              用户偏好
        → reminder.get_due_reminders()              到期提醒
        → recent_chat.get_recent_chat()             短期对话
      → deepseek.generate_with_tools()              LLM 推理（可能含工具调用）
      → imperfect_speech.add_imperfections()         不完美表达
    → _send_qq_reply()                               发送回复
    → engine.remember()                              写入记忆（异步）
      → write_pipeline.write()
        → resource_store.add()                      原始对话落盘
        → _classify()                               分类
        → _grade()                                  分级
        → _extract_keywords()                       关键词提取
        → _write_core()                             db+time+bm25+associative
        → index_vector.add()                        FAISS 追加
        → emotion.save_emotion_snapshot()           情绪快照
        → cooccurrence.add_connections_batch()      共现图建边
        → preferences.auto_extract_preference()     偏好提取
    → activity.record()                               活跃时段记录
    → common_sense.reflect_on_reply()                 社交经验自复盘
```

### 定时后台任务（每30分钟）

```
main.py:_scheduled_tasks()
  → evolve._tick()                                   过期/摘要/维护/备份
  → compressor.compress()                            记忆压缩
  → habits.match_today()                             习惯检测
  → desire_system.check_and_execute()                欲望触发
  → idle_thoughts.tick_idle_thoughts()              内在独白
  → life_stream.tick()                              生活流推进
  → engine.remember_sync()                          睡眠整合
```

### 每秒心跳

```
main.py:_heartbeat_loop()
  → state.get_breathing_state().tick()              情绪节律推进
```

---

## 十二、模块依赖热力图

按被依赖次数排序（越靠前越核心）：

| 模块 | 被依赖次数 | 层 |
|------|-----------|-----|
| `config.py` | 25+ | 跨层基础设施 |
| `utils.py` | 10+ | 跨层基础设施 |
| `db.py` | 12 | 资源层 |
| `deepseek.py` | 5 | 业务工具层 |
| `engine.py` | 5 | 插件层 |
| `write_pipeline.py` | 1（被engine组合） | 记忆单元层 |
| `retrieve_core.py` | 2 | 记忆单元层 |
| `index_vector.py` | 5 | 记忆单元层 |
| `index_bm25.py` | 4 | 记忆单元层 |
| `index_associative.py` | 4 | 记忆单元层 |
| `index_time.py` | 4 | 时间索引层 |
| `category_store.py` | 4 | 类别层 |
| `resource_store.py` | 3 | 资源层 |
| `evolve.py` | 1 | 记忆单元层 |
| `cooccurrence.py` | 3 | 记忆单元层 |
| `emotion.py` | 3 | 记忆单元层 |
| `state.py` | 3 | 插件层 |
| `common_sense.py` | 2 | 插件层 |
| `senses.py` | 1 | 插件层 |
| `desire_system.py` | 1 | 插件层 |
| `life_stream.py` | 2 | 插件层 |
| `idle_thoughts.py` | 1 | 插件层 |
| `tools.py` | 2 | 业务工具层 |
| `memory.py` | 3 | 业务工具层 |
| `identity.py` | 3 | 业务工具层 |
| `internet.py` | 3 | 业务工具层 |
| `safety.py` | 2 | 业务工具层 |

---

## 十三、关键发现

1. **五层架构大部分落地。** 资源层（db + resource_store）、时间索引层、记忆单元层（四索引 + 检索 + 写入 + 演化）、类别层均已实现。插件层是最大缺口——骨架在但血肉没填。

2. **FAISS 删除是硬限制。** `index_vector.py` 的 `remove()` 只删 Python 映射，FAISS 索引内向量残留。检索时通过 `iid in self._id_to_faiss_idx` 过滤，但会吃掉 top_k 名额。月度 rebuild 彻底清理。

3. **三层守护进程设计健壮。** guard.py → v2_ops.py → main.py，每层保下一层不死。v2_ops.py 还兼备份（SQLite 在线备份 API + MD5 分块校验）。

4. **social_intuition 是唯一真正自生长的模块。** `common_sense.py` 的 `reflect_on_reply()` 实现了对话→经验沉淀→FAISS索引更新的完整闭环。其他模块仍靠配置/规则驱动。

5. **prompt/builder.py 是系统提示的中央枢纽。** 连接了 memory_engine 下几乎所有子系统，将零散的感知/记忆/状态信息组装为白槿的"意识流"上下文。

6. **plugins/ 目前是纯门面。** 9个插件只有6个有底层函数映射，且全部是透传——零编排逻辑。`recall`/`remember`/`forget` 三个核心插件在 docstring 中列出但未实现。

7. **全量配置集中在 config.py。** 零硬编码，路径/参数/阈值/调度 crontab 全在一处。constants.py 分离业务常量。符合「同一个值只出现在一个地方」的铁律。

8. **BGE-M3 冷启动 ~20s。** 391 个权重分片 CPU 加载 + 1.7s 预热编码。FastAPI 常驻后秒回。
